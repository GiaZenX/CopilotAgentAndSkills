id: TSK-0053
product_requirement: PR-0002
derives_from: BUG-0017
type: bugfix
assigned_role: harness-implementer
acceptance_refs:
- AC-1
- AC-2
allowed_scope:
- user/claude/CLAUDE.md
- team-kits/**
- docs/reviews/**
forbidden_scope:
- .claude/**
- project_memory/**
- team-kits/kernel/**
- CLAUDE.md
required_inputs:
- 'DEC-0040 (BUG-0019 rejected: transcript reading is a legitimate diagnosis/recovery
  fallback, NOT to be forbidden), EVD-0018 (pilot measurement), BUG-0017 (entry session
  invents a non-existent /hooks ceremony), docs/reviews/2026-08-12-bug0019-bug0017-pilot.md.'
- 'MEASURED FACTS: (1) the entry file''s restart step (user/claude/CLAUDE.md step
  5, ~line 139) mentions NEITHER /hooks NOR scope approval -- the entry agent HALLUCINATED
  the /hooks trust ceremony (''hook_trust: unverified'', ''restart_required'') entirely
  on its own and pushed a non-technical persona to run /hooks. (2) The scope approval
  MINTS CLEANLY in the PM session AFTER restart (pilot phase 2: APR-0001, mint_code
  5acc16) -- the ''SDK cannot mint'' theory is refuted; the entry session simply cannot
  mint because its freshly-installed hooks are not active yet (same root as BUG-0016).
  (3) The PM skill (team-kits/dev-team/skills/project-manager/SKILL.md:135) falsely
  claims ''a normal restart never reads the transcript'' -- untrue (the model does
  read it) and it would wrongly exclude the crash-recovery case.'
expected_outputs:
- 'PART A (BUG-0017, honest entry message): at the restart step in user/claude/CLAUDE.md,
  add an EXPLICIT negative instruction -- the entry agent must NOT invent or request
  any trust / permission / /hooks / approval ceremony; the ONLY action for the user
  is to restart. State honestly that scope approval is requested and minted by the
  Project Manager AFTER restart, because the kit''s hooks activate only then. Give
  the exact honest restart message; forbid any invented security/trust step. (user/claude
  is NOT a kit-hash input -- no bump for this file, but it is deployed to ~/.claude/CLAUDE.md,
  note that in the report.)'
- 'PART B (DEC-0040, honesty line): rewrite team-kits/dev-team/skills/project-manager/SKILL.md:133-135
  so it no longer claims ''a normal restart never reads the transcript''. Honest form:
  resuming is lossless from canonical state (session_brief + active items); the transcript
  is NOT NEEDED on a clean restart but remains an EXPLICIT diagnosis/recovery fallback
  (e.g. after a crash or an unclean session end where state was not fully written).
  NO prohibition on reading -- DEC-0040. Check office-team and research-team manager
  skills for the same false claim and mirror the honest wording ONLY where it exists;
  each kit whose skill changes gets python tools/bump_kit_version.py.'
- 'TEST: a static test in tools/ that asserts no shipped manager SKILL claims the
  transcript is ''never read'' (the false-guarantee form), and that the entry file''s
  restart step forbids an invented approval/hooks ceremony -- parse the file, do not
  string-match a sentence. Red without the fix.'
- 'REPORT: file:line for every change, which kits bumped, the four suite numbers (one
  pytest tools/ -q run), ruff, validate. NOTE clearly that behavioural confirmation
  of PART A (entry agent no longer invents /hooks) requires a LIVE phase-1 pilot with
  the fixed entry file deployed -- that is the verifier''s job, not measurable by
  unit test.'
dependencies:
- BUG-0017
root_revision: 1
status: DRAFT
revision: 1
approval_ref: null
created: '2026-08-12T16:25:22'
