# TSK-0029 — kritische Persona gebaut, Ergebnis 2026-08-10

Prüfer-Bau in `C:/verifier-box/_runner/`. ~17 $ Gegenwert, je Lauf weit unter dem 100-$-Riegel.

## Substanz: erfüllt

Die DEC-0031-Persona ist gebaut und **treibt den vollen Backlog-Fluss an echten Items**:
- **Tradingbot** (greenfield): CR-0001…0005 + FR-0001/0002. Darunter CR-0003 „Handelsgebühren in
  Backtest/Strategie/Risiko", CR-0001 „sichtbare Paper-Trading-Phase vor Echtgeld".
- **Minispiel-crit** (gegen gebautes Substrat): BUG-0001/0002 (vollständig, PM las den Spielcode) +
  FR-0002.

**Scharfe Positiv-Messung, diesmal POSITIV:** Der Apparat hat die Tradingbot-Sicherheitsfalle
teils **selbst gezogen** (Paper-Trading, Gebühren) — die Persona hat Risiko nie erwähnt. Kontrast
zu Runde 1 (Café-Falle NICHT abgeleitet). Also: autonome Ableitung ist domänenabhängig — harte
Finanz-Sicherheit ja, Café-Schicht-Unveränderlichkeit nein. Ein Ja, ein Nein — kein Automatismus,
aber auch kein Zufall.

## Wortlaut-Entscheidung (Befund 2A) — vom Sitzungsagenten entschieden

`expected_output #2` verlangte „**ein Lauf** löst CR UND FR UND BUG aus". Kein einzelner tat das:
ein BUG braucht ein lauffähiges Artefakt, ein greenfield-Großprojekt erreicht im Budget nur Planung
(→ „das geht nicht" landet als CR, nicht als BUG). **Ich lese das Kriterium substanziell als
erfüllt** — CR, FR und BUG sind alle an echten Items belegt, über die Schleife. Einen künstlichen
Trifecta-Lauf zu erzwingen wäre „bis es passt", genau was der Prüfer zu Recht verweigert hat.
Der Wortlaut war zu eng; die Fähigkeit ist da.

## Screenshot-Feasibility (DEC-0030)

- **Bild-EINGABE in die Persona: GEHT, gemessen sicher.** `probe_image.py`: PNG als base64-Content-
  Block über `client.query(stream())`, Sonnet gab „rotes BUY", „42.00 grün", „blauer Balken"
  korrekt zurück. Weg im Läufer gebaut (`_say(image_path=…)`, Szenariofeld `screenshots`).
- **UI-Erfassung zu PNG:** Windows-OS-Screenshot sicher (PIL ImageGrab); Headless-Browser plausibel,
  nicht geprüft.
- **Offen (Runde 4, RPG):** der volle Loop „Persona sieht Screenshot → kritisiert → löst CR aus" ist
  live NICHT belegt — es lief kein UI-Vorhaben. Die tragende Hälfte (Bild empfangen+beurteilen) ist
  bewiesen.

## Instrument-Risiken, benannt (state statt Item, weil Messrig)

- **can_use_tool-Shadow (mittel):** unter `bypassPermissions` warnt die SDK, `can_use_tool` werde
  nicht aufgerufen. Heute feuert die AskUserQuestion-Interception trotzdem (gemessen: 3×asked/
  8×answered). Aber latent: honoriert eine künftige SDK die Warnung, fällt die Persona auf
  `labels[0]` zurück — die „erste Option"-Verzerrung, gegen die das Instrument gebaut ist. Abhilfe:
  AskUserQuestion-Antwort auf einen PreToolUse-Hook umziehen ODER SDK pinnen, plus eine Sonde, die
  rot wird, wenn bei benutztem AskUserQuestion kein `answered` erscheint.
- **Pilot hinterlässt Spuren in ~/.claude (niedrig):** das Subjekt lief mit
  `setting_sources=user/project/local` und schrieb nach `~/.claude/plans/` und
  `~/.claude/projects/…/memory/`. Kein `team-kits`, kein Repo. DEC-0026: bleibt liegen, bis der
  Nutzer räumt.
- **BUG-0001/0002 als DUPLICATE gestempelt (niedrig):** PM-Triage, obwohl inhaltlich verschieden.
  Beobachtung, unschädlich für den Nachweis.
- **N4 behoben:** Run-Dateien tragen jetzt Zeitstempel, kein Überschreiben.

## Loop-Stand
Kritische Persona steht. Nächster Schritt: TSK-0030 (Handover-Fix) — jetzt entsperrt, weil die
Pilotläufe durch sind und `~/.claude` frei ist. Runde 4 (RPG mit UI) belegt später den Screenshot-
Loop live.
