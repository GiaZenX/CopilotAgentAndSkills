# Prüfverdikt TSK-0021, 2026-08-07 — FAIL, und der zweite Messschaden derselben Klasse

Diese Datei liegt in `staging/`, weil der Kernel zum Schreibzeitpunkt erneut nicht importierbar
ist (siehe unten). Alles hier muss zu Items werden, sobald er wieder läuft.

## Das Verdikt des Prüfers (Kopie der Kernaussagen; Vollbericht im Sitzungsprotokoll)

**FAIL.** Gemessen in einer Kopie außerhalb des Repos, Gates als echte Prozesse, Schiedsrichter
`C:\Program Files\Git\usr\bin\bash.exe`.

- **F1 (blockierend):** Der H33-Fix ist durch **Quotierung irgendwo im Wort** umgangen.
  `readings` (`.claude/hooks/_harness.py:1141-1143`) markiert ein Wort mit irgendeiner
  eingespleißten Spanne als `spliced`, `_expanded` (`:1178-1182`) ruft `_tilde_prefix` dann nie —
  eine Shell entfernt Quotierung aber zeichenweise, ein `"` HINTER dem Präfix unterdrückt die
  Tilde-Erweiterung nicht. Gemessen, ein Werkzeugaufruf ohne Vorbereitung:
  `sed -i "s/a/b/" ~+/"team-kits"/kernel/state.py` → Shell schreibt, Gate rc 0. Ebenso erreichbar:
  `~+/".claude"/settings.json`, `~+/.claude/"hooks/_harness.py"`,
  `~+/"project_memory"/decisions/active/DEC-0020.yaml` — also Registrierung, Apparat und
  kanonischer Zustand. Minimaler Fix: Erweiterbarkeit des Tilde-Präfix-**Spans** getrennt von der
  Splice-Eigenschaft des ganzen Wortes entscheiden (Lesart VOR der Quotierungsentfernung); nur ein
  Präfix, in dem selbst Quotierung steht, bleibt literal (H31).
  Prüfer-Vorbehalt: gemessen mit `bash -c`, nicht mit dem Bash-Tool-Pfad des Providers.
- **F2 (blockierend):** Drei Stellen behaupten in der beruhigenden Richtung, was der Code nicht
  baut: `_harness.py:1136-1139` („that direction only ever adds a refusal"),
  `docs/POST_V2_WISHLIST.md:1844-1846` (H31 als Über-Verweigerung geführt; real Unter-Verweigerung)
  und `:1920` (H33 „blockierend, geschlossen"), `CLAUDE.md:166-169` („für jeden Aufrufer
  verweigert … Über-Verweigerung, kein Loch").
- **F3 (Ursache, mit dem Fix zu schließen):** Die Prüfmenge hat keine Achse „Quotierung im
  Zielwort": 0 von 1440 Zellen, 0 der 3×205 Tilde-Subjekte; `_tilde_prefixes`
  (`test_gates.py:2360-2367`) schließt die Anführungszeichen ausdrücklich aus dem Alphabet aus.
  DEC-0018 ist auf `MOVES` angewandt, auf das Zielwort nicht.
- **F4 (Löcherlisten-Rest):** Der Sandbox-Riegel (`test_gates.py:99-143`) leitet seine Wachliste
  aus dem Inhalt einer Sandbox nach einem Leerlauf ab, nicht aus dem, was eine Zeilenform anlegen
  kann; Skripte NEBEN `test_gates.py` deckt er gar nicht. Mechanismus, nicht Schreibweise.

Gemessene Negativbefunde des Prüfers (in Ordnung): Suite 134 grün (358 s) in seiner Kopie;
Frist-Budget 96,2 s gegen 120 registriert, H35-Ketten rc 2 innerhalb des Budgets; beide
Stolperdraht-Enden rot machbar; Zellenzahlen exakt (1440/1098/342/547/64); H36-Urteil trägt
(WinError 206 ab ~32k, Längengrenze bindet vor der Frist); H34 nennt den Mechanismus.
Ungemessen geblieben: Gate 3 mit der F1-Schreibweise bis zum Commit; PowerShell-Hälfte von F1;
Suite unter Nebenlast; Provider-Übergabeweg der Befehlszeile; `pytest tools/` (außerhalb des
Scopes); H2/H7/H11–H29 nicht erneut angegriffen.

## Der zweite Messschaden — F4s Mechanismus, am selben Tag belegt

Während der Prüfung wurde `team-kits/kernel/state.py` im **echten** Arbeitsbaum erneut
zerschrieben (jedes erste `a` je Zeile → `b`, `SyntaxError` Zeile 236; 1856 Zeilen Diff gegen den
heilen Index; git zeigt die Änderung unstaged). Schadensumfang gemessen: **nur diese eine Datei**
— `.claude/**`, `project_memory/**`, `CLAUDE.md`, übrige `team-kits/**` ohne Signatur
(`rbise|bgent|hbrness|clbude|migrbtion` trifft ausschließlich `state.py`, 39-mal).

Hergang nach Lage: Die F1-Messläufe des Prüfers (`~+/…`-Formen, `bash` als Schiedsrichter über
die Datei) liefen mindestens teilweise mit Arbeitsverzeichnis = echtes Repo statt Sandbox; `~+`
zeigt auf das Arbeitsverzeichnis. Das ist exakt die Klasse, die der Prüfer selbst als F4 meldet
und die gestern schon einmal `state.py` gekostet hat: **eine Messvorrichtung außerhalb des
Riegels, der nur Läufe von `test_gates.py` selbst bewacht.** Der Riegel hat dabei korrekt NICHT
angeschlagen — die Vorrichtung des Prüfers war nie Teil seiner Wachliste. Konsequenz fürs
Nachbesserungs-Item: Sandbox-Disziplin für Messskripte gehört als Pflicht in jeden Prüf- und
Umsetzerauftrag, und F4 steigt im Gewicht (zweiter Treffer derselben Klasse binnen zwei Tagen).

## Was daraus werden muss, sobald der Kernel läuft

1. **TSK (Nachbesserung TSK-0021):** F1+F2 schließen, F3 als Achse in die Prüfmenge, F4 als
   benannten Rest in die Löcherliste; Scope wie TSK-0021 (`allowed`: `.claude/**`, `CLAUDE.md`,
   `docs/POST_V2_WISHLIST.md`, `docs/reviews/**`; `forbidden`: `team-kits/**`, `tools/**`,
   `project_memory/**`). Der Umsetzer misst zusätzlich die vom Prüfer ungemessen gelassene
   PowerShell-Hälfte und die Gate-3-Kette der F1-Schreibweise, und prüft den Provider-Vorbehalt.
2. **TSK (Nachbesserung TSK-0020, aus der Übergabe):** F1–F3 der Migrationsprüfung, R1–R5,
   Umbenennung nach **DEC-0021** (bereits captured, VALID), Löcherlisten-Einträge aus
   `staging/TSK-0018/wishlist-entries.md` und `staging/TSK-0020/wishlist-entry-L19.md` einarbeiten
   samt Referenzen in die Docstrings der genannten Tests.
3. **EVD** zum zweiten Messschaden ist NICHT nötig als eigenes Item — der Hergang steht hier und
   geht als gemessene Begründung in F4s Löcherlisten-Eintrag.

## Wiederherstellung (aus einer Shell AUSSERHALB von Claude Code)

Gate 1 verweigert dem Sitzungsagenten jede schreibende Zeile, die `team-kits` nennt, und Gate 2
verweigert ohne importierbaren Kernel jeden Spawn (fail-closed, gestern gemessen). Von innen
nicht behebbar:

```bash
cd "C:/Offline Repos/AgentAndSkills"
cp team-kits/kernel/state.py "$TEMP/state.py.zerschrieben-2026-08-07-2"
git checkout -- team-kits/kernel/state.py
PYTHONPATH=team-kits python -B -c "import kernel.state; print('OK')"
```
