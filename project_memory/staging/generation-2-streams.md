# DEC-0057 generation 2 -- seven streams, spawned 2026-09-02 ~11:30 from 6d18407 (release 2026.09.02-10)

Cast per DEC-0059 (announced to the user, accepted: "Ja los gehts, Hygiene aber auch mindestens Opus").

| Stream | Item | Worktree (branch) | Tier | Reserved H | Scope owner of |
|---|---|---|---|---|---|
| E texts | TSK-0105 | _worktrees/g2-texts (g2/texts) | **Fable** | H105-H107 | constitutions, agents, procedure skills (all kits) |
| F kernel | TSK-0106 | _worktrees/g2-kernel (g2/kernel) | Opus | H108-H110 | team-kits/kernel/** |
| G office | TSK-0107 | _worktrees/g2-office (g2/office) | Opus | H111-H113 | office hooks, session_status.py (all kits), office templates/project_memory + repo/scripts |
| H measure | TSK-0108 | _worktrees/g2-measure (g2/measure) | Opus | H114-H116 | tools/**, docs/** only (no kit files) |
| I dashboard | TSK-0109 | _worktrees/g2-dashboard (g2/dashboard) | **Fable** (design pass) -> Opus (build, second spawn) | H117-H119 | office templates/repo/tools + dashboards |
| J hygiene | TSK-0110 | _worktrees/g2-hygiene (g2/hygiene) | Opus | none (may not write the hole list) | docs/** minus wishlist/spec/reviews; test_repo_hygiene, test_disposition |
| K humanizer | TSK-0111 | _worktrees/g2-humanizer (g2/humanizer) | **Fable** | H120-H122 | team-kits/*/skills/humanizer/**, NOTICES.md, repo_kit_owned.txt |

Handover per stream: `_round-scratch/TSK-01nn/stream-<name>.patch` + protocol in `project_memory/staging/TSK-01nn/`.
Seam items every stream reports (never writes): role/constitution sentences -> E; kernel fields/verbs -> F.

Merge round (generation 2): to be cut as its own TSK when all seven PASS; apply order by seam
weight: F (kernel) first, then G, I, K (kit dirs), then E (texts, most mirrored files), then H and J
(tools/docs only). Rules from DEC-0060: pin tests after EVERY patch; select suites by files read;
the gate-suite hole-list tripwires run in the merge; a tripwire whose one end follows from the other
is prose. Expect the hole-list appenders (6 streams) and the ratchets to conflict -- content
decisions, recorded.

DEC-0060 (g) column to fill per stream: first-report wall-clock, verifier first-pass findings,
rework rounds, tier -- Fable (E, I-design, K) vs Opus (F, G, H, J) compared on this generation.

## Log

- 2026-09-02 ~11:30 spawned all seven.
- 2026-09-02 11:46 Stream I phase 1 (Fable) done in 26 min: design package in staging/TSK-0109/, 38 sighted PNGs, EUeR parity 8/8, three self-found sighting defects fixed before handover. Lead answers: generator kit-owned (seam: repo_kit_owned.txt -> K, trigger in gate_ledger_valid -> G, .gitignore, payment-deadline field -> G); BuyPlugGo template path still owed by the user. Phase 2 (Opus) spawned 11:5x.
- 2026-09-02 12:04 Stream E (Fable) first report after ~1 h 25: 27 files, stamp -11 provisional; comment density measured on Canyon (kit code 45.8 % is the densest -> no ceiling); memory key removed from quality-engineer, research reviewer, office bookkeeper (measured shared channel); H105 closed/H106/H107. Verifier spawned 12:1x.
- 2026-09-02 12:30 Stream K (Fable) first report after ~1 h 25: three byte-identical humanizer skills, contract measured on references.declarations, tools/test_shared_skill_contract.py (6 tests, 5 mutations red), before/after pair for the user, three source attributions of the lead's research note corrected; stamp -12 provisional; H120-H122 unused. Verifier spawned 12:3x.
- 2026-09-02 12:5x Stream E verifier: FAIL, **6 first-pass findings** (Fable) — 1 blocking as naming (H105: a memory tree an older kit wrote survives the update; no installer/scaffold/kernel path names agent-memory — SEAM for kitupdate), 2 medium (mirror tripwire floor is a growing number; four-vs-five brief points), 3 low. Mechanism, scope, stamps, pins, mirrors, all four tripwires confirmed. Rework sent.
- 2026-09-02 13:1x Stream E rework 1 reported after 13.5 min (480 k tokens): all six findings worked; stamp -12 provisional; H105 verdict narrowed to "closed for NEW installations, provider behaviour with a key-less tree unmeasured"; mirror tripwire floor replaced by a definition (lead-in in >= 2 constitutions => in all three or listed with reason; 5 new entries; verifier's m4 red-measured); brief = four questions everywhere; seam 6 (kitupdate removal) = BUG-0088 captured by the lead. Runs 135 + 57/1 + 8 green. Re-verify (F1-F6 only) sent to the E verifier.
- 2026-09-02 13:0x Stream I phase 2 (Opus) first report after 1 h 13 (343 k tokens): finance_dashboard.py generator + template + dashboards/ABOUT.txt (not README: trays.py would declare the dir a document tray, measured), 13 tests/16 cases, three frozen fixtures, parity 8/8 quarters DOM vs euer_report (open items now counted per ledger year), red-first 14/14, sighting 54 images/2 self-found defects fixed, H117 (no trigger, hash chain measured), H118 (clock + scriptless), H119 (no provenance) -- office VERSION -13 provisional. Auto-regeneration NOT built: seam to G (gate_ledger_valid) + ABOUT.txt says so. Seams: K kit_owned line, G trigger + payment_term_days, .gitignore (unowned), E three role sentences. BuyPlugGo template still not received. Verifier (Opus) spawned 13:1x.
- 2026-09-02 13:2x **Stream E PASS** (re-verify, 10 min, 0 findings). (g) row E: Fable, first report 1 h 25, first-pass findings 6 (1 blocking-as-naming, 2 medium, 3 low), 1 rework round, spawn -> PASS ~1 h 55. Patch at -12: `_round-scratch/TSK-0105/stream-texts.patch` (27 files). Seams for the merge: kitupdate memory-tree removal = BUG-0088 (not this generation).
- 2026-09-02 13:3x User, on seeing stream I: "das Dashboard wird sauber mit dem Frontend Design Skill gemacht ... und auch unser backlog muss definitiv angepasst werden" -> **FR-0075** captured (backlog dashboard rebuilt the FR-0032 way: design pass + sighting + usability; kernel board vs dev-team HTML board = one renderer or two, to be measured). Follow-up round after generation 2, not a stream.
- 2026-09-02 13:4x Stream K verifier: FAIL, **9 first-pass findings** (Fable) -- 2 blocking: B1 the new mirror test skips any file present in only ONE kit copy (its own docstring names that case; mutation V1 extra file in office -> 6 suites green), B4 the before/after pair the USER judges changes facts (Getraenke -> Tee/Wasser, Arbeitstag -> Tag, "nahezu jeden" -> "die meisten", 12 -> zwoelf) against the skill's own "does not touch facts" and the pair's own "nothing changed" line; 2 relevant (B2 SKILL.md:97 "ONE illustration" vs 56 quoted literals; B3 test_reference_skills.py:103 docstring "only dev-team ships reference skills" now false, in scope); small: B5 count table two rules, B6 "6 tests" = 5, B7 G3 Modalpartikel derived not sourced (protocol only); observations for the merge: B8 scaffold_team.sh:718 repeats H83 counts (forbidden scope, seam note), B9 "never fires" over-cautious. Confirmed: byte identity, contract on references.declarations in all three kits, for_task matrix, BOTH installer twins + Codex mirror byte-equal, M1-M5 red-first, sources and licences, no dialogue, stamp -12. Rework sent (fresh Fable spawn; the first K implementer was no longer addressable).
- 2026-09-02 ~13:55 **User interrupt (Stop) killed every running subagent**: F, G, H, J implementers (~2 h each), the I verifier (26 min in), the K rework (just started). On disk nothing is lost -- worktrees: g2-kernel 17 files + patch 107 KB (12:24) + protocol 20 KB; g2-office 12 files + seam-notes (no protocol yet); g2-measure 5 files + two measurement notes + 13 mutation logs (no protocol yet); g2-hygiene 4 files + protocol 16 KB; g2-humanizer at -12 (rework not started); g2-dashboard complete, verification lost. NOTE for the merge: g2-office, g2-hygiene, g2-humanizer carry `M project_memory/.audit/hook_events.jsonl` (hooks fire inside worktrees) -- never part of a stream patch. FR-0075 (backlog dashboard redesign) and FR-0076 (EUeR category vocabulary + per-line sums + AfA hint) captured from the user's questions meanwhile; founding-year Kleinunternehmer case (25 k in the CURRENT year, immediate) is a measurement point for the I verifier restart. Restart pending the user's word.
- 2026-09-02 ~14:05 User: "Ja, alle sechs jetzt". Restarted as FRESH agents picking up the on-disk state ("Vorgefunden" section first, protocol claims verified against the tree): F kernel (Opus), G office (Opus; told to decide the two TSK-0109 seams in its area -- payment_term_days yes, the trigger as a seam with code block), H measurements (Opus; no staging protocol existed -- create it), J hygiene (Opus; full suite in the worktree is the ITEM's demand for moves), I verifier (Opus; + founding-year Kleinunternehmer fixture), K rework (Fable; same nine findings). (g) accounting: wall-clock for F/G/H/J is now two runs; note the ~10-20 min re-read cost per stream as the price of the stop.
- 2026-09-02 ~14:10 User feedback on stream I (design accepted: "gefaellt mir viel besser"): Einnahmen/Ausgaben/Ueberschuss each split Brutto/Netto/USt stacked, like the BuyPlugGo kit, so net result after VAT and the Zahllast are visible at once. Recorded verbatim + derived points in `staging/TSK-0109/user-feedback.md` (two tax states: Kleinunternehmer = no USt line as a statement, not a zero; Regelbesteuerung = full split + Zahllast; parity against euer_report / independent CSV aggregation; re-sight at 390 px). Goes into the I rework together with the verifier's findings.
- 2026-09-02 14:3x Stream I verifier (restart, 25 min): FAIL, **13 first-pass findings** (Opus) -- 1 blocking (B1: the round's core fix `open_until` has no red test, M6 mutation green; AND a real divergence: an open 2025 row reversed from 2026.csv -> page 1 / report 2 open items, both ledgers valid -- generator builds reversed_ids over ALL files, euer_report over ONE), 5 medium (M2 founding year: page says "innerhalb der Grenzen 26.000 von 100.000" -- wrong tax statement, rule exists ONLY in the generator; M3 "kein Ledger" + "ueber der Grenze" in one gauge; M4 gate test reads 6 of 8 office gates; M5 ABOUT.txt staleness claim false for back-dated bookings; M6 CURRENCY_GAP comment names a test that measures one side), 7 low (N7 counts, N8 stray csv skipped silently, N9 PyYAML traceback, N10 mirror derivation misses templates/repo/tools -> residue, N11 hole test-name checker only resolves test_gates names -> residue in .claude, N12 H119 row, N13 English validator sentence). Confirmed: scope, stamp -13, trays.py claim, parity 8/8 on fixtures, threshold `>`, determinism, robustness (BOM/CRLF/spaces/[x]), 4 of the 14 mutations + tray both ends, documented line rc 0 through all 8 gates, sighting matches. Rework (Opus, fresh spawn) sent with the 13 findings + the user's Brutto/Netto/USt split in ONE round.
- 2026-09-02 14:2x Stream K rework 1 (Fable, 36 min, 192 k tokens): all nine worked; B1 = presence check per skill name (`_shipping_kits`) before bytes, os.walk for dot-files (own find), V1/V2 red-measured; pair rewritten fact-true with the four deliberate differences LISTED (incl. "bis zu 750 ml" -> "750 ml"); SKILL.md at sha 47b12ef2 in all three; stamp -13; Suite A 59 + Suite B 12 green; patch 71 KB / 9 files. Re-verify (Opus, fresh spawn -- the first K verifier died in the stop) sent, scoped to B1-B9 + anything the rework opened. Corrected pair sent to the user for judgement.
- 2026-09-02 14:4x Stream H (Opus, run 2: 37 min, 199 k tokens; run 1 ~2 h killed) reported: predecessor's 12/14 findings held, THREE did not -- finding 14 confounded (role file never in dirA; re-measured: registration re-read, role binding STAYS), the VS Code answer was WRONG (extension runs the binary with --input-format stream-json / ENTRYPOINT sdk-ts, where /cd "isn't available"; the switch exists as `set_cwd` control message and lands WITHOUT trust fields for a previously trusted dir -> H114 limit widened), two dangling references. Answers: CLI yes (`/cd <abs path>`), VS Code no for /cd, Desktop unmeasured (not installed). `--continue` binds the new agent. BOM tripwire both ends + counter-test; test_model_pins.py; 7 mutations red; no stamp (no kit file). Known over-strictness named: counter-test would go red for a skill shipping a reference .md -- potential MERGE CONFLICT with K's mirror rule. Seams: kit hook for the visible pin fallback, README BOM note, the CLAUDE.md "binds at session start" sentence, restart ceremony wording. Verifier (Opus) spawned.
- 2026-09-02 15:0x Stream K re-verify (Opus, 33 min): **FAIL** -- 8/9 closed, B4 reopened, **5 new** (N1 new docstring claims KIT_SPECIFIC_HOOKS files exist in all three kits -- format_on_write.py is missing in office, AND test_hooks.py:2453 still has the presence-blind `len(copies) < 2` cut = the hook-side twin of B1; N2 "a definition, a count and a move / the count is the check" -- 8 of 17 blocks carry no count; N3 four more unlisted assertion changes in the pair ("die Flasche" -> "Der Koerper", kann->haelt, kann->imperative, sollte->imperative); N4 "ten stems" = eleven, "one rule per row" false for two rows; N5 protocol §1 numbers stale + old sha; N6 the named residue points at the wrong line). B8 withdrawn by the Opus verifier (scaffold comment is dated, was true on 09-01). Pattern: each Fable fix brought one new universal claim of the same class. Rework 2 sent to the same Fable agent (context kept; tier unchanged, announced) with H120 (hook mirror presence blindness) and H121 (two derivation limits) to write; measure-every-new-claim rule added.
- 2026-09-02 15:1x Stream H verifier (Opus, 32 min, 262 k tokens): **FAIL, 10 first-pass findings** (Opus implementer, two runs) -- 4 blocking: B1 H114 still claims the CLI dialog as a limit, but m4.log shows a later /cd into an already-trusted dir is dialog-free (limit = FIRST switch only, both surfaces); B2 the BOM counter-test's over-strictness (references/*.md, agents/README.md go red) is measured but written NOWHERE, and a definition WAS possible (agents-dir any depth OR SKILL.md; 32+29+0) -- live merge conflict with K's mirror rule; B3 pin-reader docstring "a client reads a role out of a directory with that name" false: nested `.claude/agents/nested/agent-z.md` loads and spawns (vnested.log) -> nested pins unchecked, both test ends share the narrow predicate; B4 the path correction is itself wrong (four ext/reb jsonl and cdrig/hooklog.jsonl do not exist; evidence sits in ext1-before.jsonl). Medium: M1 `sdk-ts` misread (extension's `wQ` sets `claude-vscode`; result unchanged, 3 variants), M2 the extension ships "Open in Terminal" where /cd works, M3 the recommended round-trip passes through a dir without .claude. Low: 1046->1050 files, "alle" banners, N3 item-level (forbidden_scope should except staging/<id>/ -- gen-3 template note). Confirmed: reb1 + ext1 independently reproduced, 11 mutations red, correction discipline "vorbildlich", seams complete. Rework sent to the same Opus agent.
- 2026-09-02 15:3x Stream G (Opus, run 2: 84 min, 287 k tokens; run 1 ~2 h killed) reported: predecessor's build held in substance, SIX defects found and fixed red-first (test pointer to wrong file; unqualified test names in H111/H112 -> test_gates red; F6 .gitignore unmeasured; "ships nothing that sends" prose -> smtplib mutant test over 44 modules; MAX_LEDGER_YEARS 12 vs comment 10; max-measurement in the cheap direction); three prose claims withdrawn. FR-0034 duties register (5 feeds, 12 red tests as process), FR-0038 derived run record + H112 chain, FR-0028 recall measured (rc 2 empty / hash / holds in session 2 / rc 2 bypass / rc 2 other repo), FR-0002 F2-F6 + F7/F8 table. **Deviation from the item reported:** session_status.py NOT mirrored (KIT_SPECIFIC_HOOKS entry with reason, both tripwire ends) -- verifier told to measure whether FR-0038's overdue notice belongs in all three kits (project-auditor x3). TSK-0109 seams: trigger NOT wired (generator not in tree, forbidden), code block in protocol; no second payment field (exists as receivables.payment_terms_days). Stamp office -13; patch 14 files. Verifier (Opus) spawned.
- 2026-09-02 15:4x Stream H rework 1 (Opus, 27 min, 288 k tokens): all 10 worked -- B2 = `is_a_definition_a_client_loads` (role = .md under agents/ any depth; skill = file NAMED SKILL.md; minus `opens_as_prose` = first line a markdown heading) + both-loaders assertion (32 roles + 29 SKILL.md of 1050 tracked), README case measured in the rig (client does not load it, readme1.log); B3 = `under_an_agents_directory` on path segments, both test ends share it, nested agent-z re-measured; B1 = H114 limit narrowed to the FIRST switch (pty-m4/m4.log); B4 = raw-data table + `check_refs.py` resolving 58 backticked evidence spans, 0 missing; M1 offsets confirmed + full-args rerun (/cd still unavailable in the extension); M2/M3/N1/N2 done; one self-found inaccuracy (AGENT-A/AGENT-B block) corrected. Red-first 6 cases incl. B2-c/d as counter-probe. Patch 1529 lines / 6 files, no kit file, no stamp. Open, named: terminal.open not driven; set_cwd handshake on an unknown dir; `opens_as_prose` edge. Re-verify (Opus, fresh spawn) sent with a 12-point list: attack the predicate (blank first line, HTML comment, README without heading, misplaced frontmatter after `# Titel`), mutate both loader halves, B3 segments incl. backslash, redfirst2 + OLD test vs NEW fixture, H114 wording in three places, check_refs' complement (evidence outside backticks), M1 offsets, apply --check on main HEAD, prose vs code of the NEW sentences, seam with K's reference-file allowance.
- 2026-09-02 16:0x Stream K rework 2 (Fable, 36 min, 275 k tokens): N2 = SKILL.md "a definition, a check and a move -- the check is a count where countable, otherwise the definition read against the text" (17/17 blocks move + definition, 9/17 count verb; own first version "otherwise a question" fell at its own measurement); N1 docstring fixed + H120 written (three chains: missing hook -> registered-shipped test red; extra hook -> documented test red; shipped+documented+unregistered -> no test sees it; presence half NOT built, reason in test_shared_kit_files_identical docstring; verdict "no hole with chain, dead weight"); H121 -> mechanism instead of hole: new test `test_a_reference_skill_ships_in_every_kit_that_ships_a_role_it_names` (`_kits_owed` from the declaration's roles; red-first: office copy moved -> 1 failed while the mirror test stays green); pair: eight listed differences, HEDGE 5->1 traced, v_pair2.py; N4/N5/N6/B8 in staging; three stamps (-14/-15 own line-length errors, -16 final), sha b2cd9c0c x3, 0 CRLF; Suite A 60, Suite B 12/901; patch 83.5 KB / 11 files (+wishlist, +test_hooks.py docstring). H121/H122 unused. Re-verify 3 (Opus, fresh spawn -- the second verifier is no longer addressable) sent, 11-point list: independent sentence diff of the pair vs the eight-point list, m_countclaim + "definition read against the text" as a new property claim, attack `_kits_owed` (empty roles, misspelled role, reverse direction), H120 chains + runtime effect of an unregistered hook, "office elf eigene" via comm, len<2 cut docstring, identity/stamp/apply --check, every §9 one-liner, seam with H's SKILL.md-named definition. DEC-0059 note: if this round again yields prose findings, K switches to Opus for rework 3 (announced to the user earlier).
- 2026-09-02 16:1x Stream I rework 1 (Opus, 1 h 26, 402 k tokens, ~40 min of it test runtime): all 13 findings worked (10 with mechanism + red test, 2 named residues, 1 text) + the user's Brutto/Netto/USt split on Ueberblick AND EUeR, one renderer, `data-figure` names. Own measurement changed the design: a row-wise split printed "Zahllast -6.077,56" for the `regular` fixture (income kleinunternehmer, expenses standard) = a Vorsteuer refund that section 19(1) UStG excludes -> a `tax_state` with three cases (Regelbesteuerung full split + Zahllast; section 19 = sentence, no Zahllast; alarm = "USt nicht belastbar" with the report's informational figures beside it). DEVIATION from user-feedback.md named: alarm state is NOT rendered as Regelbesteuerung (would claim a 5.932,60 refund; no project file says whether VAT applied for the period). `kleinunternehmer: null` -> full split + "Steuerstatus nicht hinterlegt" (verifier told to judge whether a Zahllast figure there is an honest statement). Parity three-way (report Brutto+USt per quarter; Netto via independent CSV aggregation), A1-A3 red. B1 = two readings `rows`/`report_rows` + `crossyear` fixture, page names the divergence. M2 = `previous_unknown` state (profile carries no founding year -> statement refused, seam G `founding_year`). M4 = gate test runs the REGISTERED command lines (old reading 6 gates rc 0, new rc 2). M5 stale claim re-measured (sha byte-identical after a back-dated booking). N10 = mirror dirs derived from templates/repo/ dirs shipping *.py (closed). N11 stays: `.claude/hooks/test_gates.py` name checker -> MERGE ROUND scope (Edit tool by the implementer; not "outside Claude Code" as the report says -- gate 1 refuses .claude/ to the LEAD, H80 refuses shell lines naming a hook file to everyone). Fold at 390: KPI block ends y=773/776/783 (was 658), first booking below the fold before and after. 98 images / 5 fixtures (+`crossyear`, `founding`), 7 sighted by the implementer. Five verifier mutations re-run: all red now (M6 1 failed, was 16 passed). Stamp office -14; patch 305 KB / 25 files. SEAM for the merge: I and K both patch `tools/test_hooks.py::test_shared_kit_files_identical` (I derivation, K docstring + `_shipping_kits`) -- verifier measures apply order I then K. Re-verify (Opus, fresh spawn) sent with a 13-point list, first of all the tax statements per state (null profile, reverse-charge/exempt rows, refund sign, alarm wording + legal basis section 19(1) s. 4 UStG), then three-way parity, B1 trap, founding wording, M4-M6 mutations, N9 rc 1, sighting incl. re-render, scope/stamp/apply --check.
- 2026-09-02 16:3x Stream G verifier (Opus, 36 min, 225 k tokens): **FAIL, 16 first-pass findings** (Opus implementer, two runs) -- 13 package + 3 item. Blocking: P1 `_duties.py` docstring and H113's "was stattdessen begrenzt" claim "never accumulates past obligations", measured: 13 retention items for a 2005-2017 archive under ONE rule push the single due USt-Voranmeldung out of the 6-line briefing (no test); P2 the shipped filing_plan.yaml's two "rules every plan needs" (FP-900/901) carry retention strings the register cannot read -> "DEADLINE REGISTER INCOMPLETE" at EVERY session start; P3 the template (and seam E2) claims guard_fs_tripwire refuses "every shell deletion under archive/" -- python -c os.remove / find -delete / tar --remove-files rc 0, the guard's own head names two of them. Hole-list residues: P4 FR-0038 delivered in 1 of 3 kits (feed is kit-independent, project-auditor x3, supplier hooks byte-identical; mirror decision itself RIGHT, both ends red), P12 DELETE_VERBS enumeration (`find -delete` in no list), P11 day boundary unmeasured (3 mutations green). Rework: P5 MAX_LEDGER_YEARS sentence (11/13 both green; the slice is what is pinned), P6 drive-letter guard positional (`a/b/D:/<year>` walks D:), P7 "ships nothing that could send" corpus misses the installed kernel (lock.py imports socket), P8 business_profile.yaml:79 comment names Shopify (in scope, unfound), P9 recall STEP 5 cannot fail (literal), P10 session_status swallows a missing _duties silently, P13 audit line not in patch (correct). Item findings: I1 allowed `*/hooks/session_status.py` vs forbidden dev/research `hooks/**` (contradiction), I2 "mirrored byte-identical" vs the older KIT_SPECIFIC_HOOKS exception (implementer decided right), I3 "a product group" undefined. Confirmed: scope, stamp -13, apply --check on 6d18407, five feeds as process, 11 failure modes refuse without traceback, H111/H112 reproduced, pointer test 184/0, smtplib mutant red, .gitignore 3 mutations red, neutrality mutations, 8 + 60 tests. LEAD decisions: (a) item TSK-0107 expected_outputs re-cut through the kernel (update: property "current obligation only" as a TEST, office-only FR-0038 here, KIT_SPECIFIC exception stands, neutrality defined over raw lines, P2/P3 named); (b) FR-0038 for dev/research = its own follow-up TSK (mirrored `_routine.py`, precedent _audit.py, called from all three session_status.py; measured as a process on scaffolded dev + research pilots), dependencies TSK-0107, same worktree, same implementer after the rework; (c) P4/P11/P12 residues unnumbered in the protocol -> merge round numbers them (H123+). Rework order to a fresh Opus implementer (the run-2 agent is no longer addressable).
- 2026-09-02 16:5x Item re-cut through the kernel: `update TSK-0107` refused (work-order fields frozen outside DRAFT; remedy text says "transition back to DRAFT"), `transition TSK-0107 DRAFT` refused (READY allows only CANCELLED/LEASED) -> kernel BUG to capture (remedy not walkable; belongs to F territory, not gen 2). Path taken: TSK-0107 stays as the historical order; **TSK-0113** = re-cut office order (allowed_scope without `*/hooks/session_status.py`, so the I1 contradiction is gone; 8 expected_outputs incl. the "current obligation only" PROPERTY as a test, KIT_SPECIFIC exception standing, neutrality over raw lines, P2/P3/P10 named, residues unnumbered -> merge round); **TSK-0112** = FR-0038 routine feed for dev/research (`_routine.py` mirrored x3, precedent _audit.py, called from all three session_status.py, measured as a process on scaffolded dev + research pilots; dev/research VERSION -11 provisional). Both READY, index regenerated. G rework spawned (Opus, fresh -- run-2 agent not addressable): TSK-0113 first, then TSK-0112, same worktree g2-office, ONE patch at `_round-scratch/TSK-0113/stream-office.patch`, protocols under staging/TSK-0113 + TSK-0112. Lead decisions in the order: P12 = one line in the guard's own head (no mechanism), P7 = kernel named as measured unchecked residue (out of scope), trigger code block gets the kit_owned condition INTO the block, 200-rules-no-past-years budget measured once.
- 2026-09-02 17:0x Stream H re-verify (Opus, 40 min, 214 k tokens): **FAIL, 12 findings (B 3, M 2, N 7)**, none a hole (H114-H116 confirmed in substance: chains, quotes, evidence re-measured). B1 `opens_as_prose` subtracts NOTHING on the tracked tree (census 61 = 32 + 29, skipped []) and no test notices it switched off (11 passed either way); B2 the subtraction is ONE spelling of "document" (Setext / prose README still demanded frontmatter; measured against the real client 2.1.258: neither is loaded; client rule = first line `---`, `# Titel` above frontmatter NOT loaded either -> the rework's direction was right); B3 the BOM doc still describes the pre-rework predicate. M1 "/cd occurs zero times in the bundle" false (14 hits, all in a MIME table; conclusion holds); M2 H114 table row "measured on both ways" stronger than body + doc (set_cwd way has no dialog at all; unknown-dir handshake unmeasured). N1-N7: stale count "two limitations", 53 vs 58 spans, two misquotes, offsets are CHARACTER offsets (+ `e0` not `U`), "a human always writes", "a client reads ... at any depth" over-claim (docs/agents/ friction), two modules two role notions. Verifier's own incidents: (1) it altered the WORKTREE INDEX (robocopy carried the worktree's .git FILE; its `git add -A` hit .git/worktrees/g2-measure/index; contents + `git diff HEAD` unchanged, index rebuilt from ls-files) -- implementer told to re-measure and `git add -N`; (2) a byte-vs-character offset finding withdrawn by itself. Confirmed: B3 depth both ends, both-loader assertion red from both sides, red-first table line by line, BOM tripwire own red run (3 cases), hole-list guards can fail (3 plants), doc counts 61/10/3, 1050 files, banners, AGENT-A/B cause, H114 chain, check_refs 58/0 + unbackticked names all resolve, apply --check on the base built from the reverse patch, no kit file, ruff/15/11/8. Seam to K: compatible (SKILL.md name key vs references/ mirrored via _skill_files_by_path); only touch point = both append to POST_V2_WISHLIST.md. LEAD decision for B1/B2: remove `opens_as_prose`; role key = LOCATION (any tracked .md under an agents dir), skill key = NAME; the client's first-line-`---` rule must NOT become the subject (a role that lost its separator would drop out silently); the over-demand on documents placed among roles is named friction with probe2.log as the measurement. Rework 2 sent to the same Opus implementer (context kept).
- 2026-09-02 17:1x Stream K re-verify 3 (Opus, 51 min, 231 k tokens): **FAIL, 8 findings (B 2, M 2, N 4), 6 of them prose-claims-more-than-code**. B1 the new H120 entry turns `.claude/hooks/test_gates.py` RED (four bare tools/ test names in backticks; the name checker resolves bare names against test_gates.py only; base state green) -- lesson: test_gates runs whenever a file it READS changes (HOLE_LIST), not only when a gate is touched; B2 the pair's deviation list is incomplete for the THIRD time (word-set diff: "robustem", "innovative", "einzigartige" removed from surviving sentences, in no bucket; plus causal "ermoeglicht" -> "laeuft nicht aus", "nur 380 g", "bequem", inserted "schon") -- the list is an enumeration of what was noticed, not a derivation; M1 SKILL.md heading/`:84`/`:98-100` still carry the count-is-the-check claim (8/17 measured, G4 miscounted; "countable" refuted by the pair table itself); M2 "eleven" in a test_hooks docstring beside the file's own precedent against counts; N1 §9 one-liners do not measure what the sentences say (13 hand-picked probes for "complete"; 17/17 definition sentence unmeasured); N2 three stale protocol sections; N3 H120 table row equates the cut with an example that lies in two kits; N4 ITEM defect: "the user's real shop copy" lies in user/** = forbidden (lead accepts; the pair is one the user repeats on his text). Confirmed: H121 rightly unused, three `_kits_owed` attacks all caught by neighbours, six red-first chains, H120 chains + verdict against 29 real hook processes on a scaffolded project (dead weight holds; shadow import rc 2), identity/stamp -16/check/validate/ruff, install + Codex mirror, patch byte-identical, scope, Suite A 60 / B 12, seam to H consistent. Side observations for the merge round: a hook file started from OUTSIDE the repo passes gate 1 rc 0 (H80 derivation does not reach copies); scaffolded office registrations carry no `timeout` (kit side, FR-0057). (g) row K so far: Fable first report 1 h 25 -> 9 findings; Fable rework 1 -> 6 (5 new); Fable rework 2 -> 8 (6 prose); **tier switched to Opus for rework 3** (announced to the user before). Rework 3 spawned (Opus, fresh): B2 closed by a mechanical word-set diff (`devlist.py`, three buckets, 0 unassigned = the claim), B1 prefixes + hole-list test selection in a copy, M1 wording from the parsed 8/17, M2 count removed, N1-N3.
- 2026-09-02 17:3x Stream I re-verify (Opus, 2 h 15, 242 k tokens): **FAIL, 10 findings (B 2, M 3, N 5)**, both B at the fix itself. B1 `tax_state` decides section 19 on ONE spelling (`is True`); `"true"`, `Ja`, missing tax block, `null` (= the SHIPPED template value) all print "USt-Zahllast -6.077,56" = the refund the docstring itself calls impossible, and the "Steuerstatus nicht hinterlegt" notice hangs on `is None` so it is absent on the visible tab for the other spellings; comment ":352 the rows carry the answer" claims protection the code does not build. B2 the EUeR tab prints "Zahllast: -6.077,56" four lines above "es gibt keine Zahllast" (regular) / -5.932,60 (alarm), and the sentence claims the report prints these three numbers -- euer_report prints two, never a Zahllast. M1 N9 test pins rc equality, not refusal (SystemExit(0) mutation green); M2 reported selection 35 -> measured 31 and `test_shared_kit_files_identical` not in it; M3 gauge "laufendes Jahr" in previous_unknown still prints "26.000 von 100.000, Rest 74.000". N1 plural() missing at four places ("1 Buchungen" visible in founding-ueberblick-1280.png, which the sighting list did not include); N2 legal basis: since 2025 section 19(1) makes the turnover tax-FREE, the Vorsteuer exclusion follows from section 15(2) s.1 no.1 UStG, not section 19; N3 fixture "GmbH" (no EUeR for a GmbH; template comment names GmbH as an example value -> SEAM to G, sent to the G rework); N4 doc describes a version that does not exist; N5 mirror derivation anchored on .py while .html is compared (empty class today). Confirmed: reverse-charge/exempt follow euer_report, refund under real Regelbesteuerung with sign sentence, alarm wording + the deviation from the order HOLDS, three-way parity 16 quarters, A1-A3 red, B1 counting with the sentence, founding verdict, M4 both halves, H117 chain, M6, N10 both ends, 98 PNGs byte-identical re-rendered, fold numbers, scope/stamp/patch vs 6d18407 AND c4f3cc0, ruff/validate, 23/9/8; N11 residue confirmed open. MERGE NOTES: (1) I x K collide in the SAME docstring of tools/test_hooks.py (@@ -2821): I replaces the two `_assert_mirrored` lines by the `_shipped_code_dirs()` loop, K appends a paragraph citing `_assert_mirrored("templates/repo/scripts", ...)` -- hand merge; also POST_V2_WISHLIST.md:2350 and office VERSION; (2) `main` is at 159dcbe (07-18) -- the merge target is `feat/harness-v2` (c4f3cc0); (3) office settings.json registers all 13 PreToolUse hooks WITHOUT `timeout` (FR-0057 territory, kit side). LEAD decisions for rework 2 (same Opus agent, context kept): three tax states by DEFINITION (True / False / everything else = unknown, treated like previous_unknown on every tab); no "Zahllast" figure printed where applies is false, only the report's two numbers; gauge unknown in previous_unknown; `assert rc != 0`; selection `or shared_kit`; plural at all four; section 15(2) basis; fixture Einzelunternehmen; sighting = every tab of every fixture SEEN; N11 residue wording corrected (Edit tool by an implementer in the merge round, not "outside Claude Code"). (g) row I so far: Opus build 1 h 13 -> 13; Opus rework 1 1 h 26 -> 10 (2 B at the fix).
- 2026-09-02 17:4x Stream H rework 2 (Opus, 27 min, 364 k tokens): index incident measured harmless (HEAD, diff 6/1479 identical; `git add -N` restores MM/AM; protocol 8.0). Base measurement `plant_shapes.py`: five opening shapes under agents/, the client's `init` event names what it loads -- only the separator-first file; ATX/Setext/prose/HTML-comment all rejected -> `opens_as_prose` knew one of four rejected shapes. B1 clause removed; B2/N5-N7: `is_a_definition_a_client_loads` = two clauses, no subtraction, role half IMPORTED from test_model_pins (`is` identity measured), friction named with cases3.py and the remedy "move the file, not widen the check"; B3 doc rewritten; red-first table rebuilt (8 rows incl. SHARED proof for N7 and FRICTION as friction, not coverage); M1 14 hits at all five places; M2 H114 row splits /cd vs set_cwd + new `check_limits.py` extracting the same five statements from row/body/doc §2 (first run red, §2 aligned); N1-N4 fixed; self-found: three ASCII transliterations, 5 unbackticked `log_hook.py` mentions = quoted lines, named. Runs 15/11/8, check_refs 58/0, patch 1709 lines / 6 files, no kit file. Re-verify 2 sent to the same Opus verifier (context kept; told to copy WITHOUT the .git file this time), 8-point list incl. attacking check_limits (string search vs statement) and the SHARED narrowing both ways.
- 2026-09-02 ~17:05 **Rate limit (session limit, reset 17:10 Europe/Berlin) killed all six running agents** mid-turn: F (kernel, sharpening the ratchet reader), J (hygiene, reading state), G rework (at the neutrality test), I rework 2 (at B2), K rework 3 (reading the item), H re-verify 2 (at start). On disk nothing lost (worktrees + protocols). User: "weiter machen". All six RESUMED via SendMessage with context kept (not fresh spawns): each told to measure the on-disk state first (`git status`, `git diff HEAD --stat`, protocol vs tree, suites on the intermediate state) and to note the cut in its protocol. (g) accounting: add the re-read cost of a resume (small; transcript kept) -- distinct from the 14:05 Stop, which forced fresh spawns.
- 2026-09-02 17:5x Stream J (Opus, run 2 resumed after the cut: 7 min this leg; run 1 ~2 h killed at the Stop, run 2 ~1 h 40 before the cut) reported: predecessor's move re-measured independently (exactly two hits), FOUR defects in the inherited package fixed red-first (reference corpus was five named dirs vs the whole tree -> now `git ls-files -c -o --exclude-standard`; a comment claimed the check could not fail without the inclusion clause -> mutation green -> now a test; wishlist pass §3 off by one item (drawio in BUG-0074); a predecessor's pytest still running against a changing tree, killed). Output 1 wishlist pass §1-10: 8 thin FRs, 6 pointers to write back, 2 supersessions. Output 2 moves: ONE file (docs/pilot/2026-08-09-drehbuecher.md -> docs/archive/2026/pilot/), the shipped instrument names exactly two remaining unwired files, both unmovable (HANDBACK.md = open instruction; a review in forbidden scope). Output 3 tripwire as two definitions: every archived file named by exactly one index above it (both ends; live red on first run: four files since 08-13 without an entry -> docs/archive/README.md new) + every EVD artifact_ref still resolves (80 refs, both roots; the measured TSK-0022 incident). 21 mutations all red. FULL tools/ suite in the worktree (the item's demand): 4161 passed / 14 skipped / 0 failed in three parts (~2 h 02, one 74-min run killed by the environment at 86 % while four other pytest sessions ran); gate suite 488 + 1 red = the BUG-0033 timing test, red 4/6 on a clean 6d18407 too (not this round's). Patch 48.9 KB / 4 files, APPLIED on a clean 6d18407 (rename survives), no kit file, no stamp. Named, not closed: F1 HANDBACK.md patches no longer apply (BUG-0012/0014 TRIAGED), F2 "zwoelf Dateien" count rotten (15; wishlist forbidden), F3 445 KB raw logs under docs/reviews (forbidden), F4 07-24 review unwired 40 days (forbidden), F5 artifact_ref written against two roots, deliberate under-delivery: docs/research/2026-08-16 model-downgrade note stays (dir wired by a live test; the hole list calls a git mv there the user's decision). Verifier (Opus) spawned.
- 2026-09-02 17:5x Stream F (Opus, run 2 ~3.5 h incl. ~1.4 h test runtime and the rate-limit pause; run 1 ~2 h killed at the Stop) reported: tree byte-identical with the 12:24 patch, protocol correct except its own open "see addendum" (two real red tests from run 1 found in scratch, both fixed: staging.py refusal spelled `evidence --artifact-ref` without the four required args). BUG-0083/84/85/86 + FR-0040 from run 1 verified; NEW: FR-0037 (`PORTABLE_PATH_MAX_CHARS = 240`, warned once per tree), FR-0054 (`related_sr` optional, root-membership checked), FR-0018 (`report.similar_items`, stderr hint at capture, never blocking; threshold 0.45 MEASURED on the real 97 items: honest neighbours max 0.314 / n=3686, re-submissions min 0.493), FR-0039 (INV.verified producer `record_invariant_verification`, merge blocker, `verify-invariants`, measured at the live hook rc 2 -> test written -> rc 0; third answer "undecidable" = warning so a non-Python project is not locked forever), FR-0017 (outline AREA as an attribute, not a node type -- measured: a node type is a prefix in `guard_no_adhoc.ITEM_TYPES`, which is forbidden scope), FR-0067 (`revise-document` + approval kind `document_revision`, 7 mutations red). Two own defects found by own tests (revision card truncated the new value; FR-0039 fail-closed would lock non-Python projects). H109 (collectable = parsed, not run), H110 (unreadable check = undecided), H108 unchanged. SEAM S1 BLOCKS THE SUITE: three constitutions + README do not name `freeze-report`, `verify-invariants`, `revise-document` -> `test_every_span_that_presents_the_command_surface_names_all_of_it` red (E/merge); S7-S9 new. Runs 654+59+154+488+898; final test_hooks 898 passed / 3 failed = 2 TimeoutExpired under load (isolated 4 passed) + S1. Stamp -15 x3 (three stamps). Patch 3974 lines / 25 files / 241 KB, no new files, no audit line. Verifier (Opus) spawned.
- 2026-09-02 18:0x Stream H re-verify 2 (Opus, 26 min work, 282 k tokens): **FAIL, 6 findings (B 1, M 1, N 4)**; 10 of 12 earlier findings closed, index incident clean this time. B1 the H114 BODY (:6791) still says "auf beiden Wegen dasselbe" while row and doc §2 now mark the set_cwd unknown-target case unmeasured -- and `check_limits.py` passes it by accident (its "unknown target" cell matches an unrelated "nicht gemessen" sentence; replacing the wrong sentence by the right one changes nothing) = a named check that cannot fail (SR-0008). M1 shipped test code cites scratch evidence (`shapes1.log`, `plant_shapes.py`, the VERIFIER's own `verify2/probe*.log`, `cases3.py`) and the durable doc does not carry the five-shapes measurement. N1 "wider than ANY client reads" + cases3.py measured something else; N2 "cannot drift apart" holds only via ruff F811 for the same name; N3 the depth property has no holder on the shipped tree (narrowing to level 1 -> 15 passed); N4 53 vs 58 in one protocol section. Rework 3 sent to the same Opus implementer (context kept): body wording + target-bound check_limits cell with both mutations red; five-shapes section into the measurement doc, docstrings point there; N1-N4.
- 2026-09-02 18:1x Stream K rework 3 (**Opus**, 31 min, 249 k tokens): B1 seven prefixes in the H120 entry (+ two spans that lost their spaces under whitespace stripping), gate selection red-first 1 failed -> 8 passed; B2 the pair document now carries a DECOMPOSITION (25 rows: template section | after section | kind, three kinds, 15 numbered points) produced by `devlist.py` reading both texts and the table from the same document: 111 missing words -> deviation 44 / struck 64 / unchanged 3, 0 without a row; the verifier's own p_devlist reports 0 unlisted; 5 mutations red; M1 heading/`:84`/`:98-100` rewritten, 8/17 parsed, the NUMBER deliberately kept out of the kit file (would rot; pointer to a throwaway instrument) -- in protocol §10.2 only; M2 count removed + a second same-class sentence in the same docstring fixed; N1-N4 done. Stamp -17 x3, sha 3b1efc2c x3, Suite A 50 + gaplog 10, Suite B 12/901, gate selection 8, patch 84.8 KB / 11 files byte-identical with git diff. NEW residue named: the gate's hole-list name checker mis-pairs backticks across CODE FENCES (a bare unresolvable name after a fence is not reported; measured with p_spans.py) -- same checker as I's N11 residue; verifier asked to judge H121 vs merge-round residue. Re-verify 4 sent to the same Opus verifier (context kept).
- 2026-09-02 18:2x Stream H rework 3 (Opus, 16 min, 401 k tokens cumulative): B1 body aligned with row and doc §2; `check_limits.py` cell bound to the TARGET + a second cell against "one limitation for both ways"; red-first with a CONTROL line (removing the unrelated "nicht gemessen" sentence now stays green, the verifier's blind mutation now red); M1 five-shapes measurement as §5a in the durable doc, docstrings point there, a self-introduced vertical tab caught before the patch; N1/N2 wording; N3 new holder `test_the_role_predicate_reaches_any_depth_and_stops_at_the_directory_name` on literal paths (narrowed + holder 1 failed / narrowed without holder 14 passed = the finding); N4 superseded. Runs 16 / 11 / 8, check_refs 65/0, check_limits 0; patch 1884 lines / 6 files, no kit file. Re-verify 3 sent to the same Opus verifier (context kept), incl. an attack on the new "both ways" cell (statement vs word).
- 2026-09-02 18:3x Stream J verifier (Opus, 29 min, 200 k tokens): **FAIL, 10 first-pass findings (B 2, M 1, N 7)** (Opus implementer, two runs). B1 the wishlist pass says "eight thin FRs, six pointers" while its own table and prose count 7 / 5 / 2 -- the lead works from this list; B2 `_archive_files()` walks the filesystem with a skip list (contradicting the package's own `_carried_files` argument): an IGNORED `Thumbs.db`/`build.log` under docs/archive/ turns the new hard test red; M1 `assert answered == {"state","repo"}` makes the F5 defect (two rooting conventions in the store) load-bearing -- repairing F5 turns the suite red and the message points at the reader. N1 corpus = git-carried files (a mention in an ignored file is invisible; today no effect) -> merge-round residue; N2 comment claims a header-derived column, code reads cells[2]; N3 class TEST = "any tools/.py names it" (three non-test scripts; one doc became TEST through this round's own docstring); N4 two growing numbers in comments; N5 protocol numbers already off (1028/30, 80 not 81); N6 the pointer rows carry no pointer text for the lead; N7 audit line correctly outside the patch. Confirmed: move clean (one hit), 21 mutations red with both tests red on M-F, own index/ref cases at both ends, instrument names exactly two unwired files, research-dir wire + user-decision line, collect 9->19 and 913+2235+1027 = 4175 = 4161+14, timing test red on clean 6d18407 (4.74 s), patch/scope/apply --check on both bases, backticks 0 unresolved of 7636 symbols, wishlist spot checks incl. F2 (15 files, not twelve). Rework 1 sent to the same Opus implementer (context kept): derive the summary from the table, cut the archive walk against `_carried_files`, M1 fixed now (synthetic both-roots + non-empty), N1 docstring limit + residue, N2-N6; full suite NOT re-run (no move changes).
- 2026-09-02 18:4x Stream I rework 2 (Opus, ~50 min incl. the rate-limit cut at 16:35 -- resumed 17:16 with B1 in the tree and B2 half done, suite 1 failed / 22 passed at the intermediate state, continued from there): B1 tax state by DEFINITION (True / False / everything else = unknown, treated like the undecidable threshold case; sentence on every tab with data-figure + in "Jetzt ansteht"; 22 red cases); B2 no difference and no "USt-Zahllast" label where applies is false, EUeR tab prints only the report's two numbers; M1 rc != 0; M2 selection `or shared_kit` (implementer counts 35 -> 37; verifier had measured 31 -> to be reconciled); M3 gauge unknown in previous_unknown; N1 plural at four places; N2 legal basis section 15(2) s.1 no.1 (section 15(3) excludes section 19), read 2026-09-02; N3 fixture Einzelunternehmen; N4; N5 derivation .py and .html, both ends. Two sentences made false by the fixes corrected. Red-first 13/13, baseline 40 passed; 98 images, 39 SEEN (every tab of every fixture at 1280 incl. scriptless; Ueberblick + EUeR 390 dark). Runs 40 / 9 / 37 / 8; stamp office -15; patch 333 KB / 25 files. N11 wording corrected; I x K seam resolution proposed in four points; seams to G: shipped `kleinunternehmer: null` (every fresh project starts "unknown" until the interview asks -- question + template comment verbatim in the protocol), legal form unchecked. Re-verify 2 sent to the same Opus verifier (context kept), incl. new spellings (1/0/"false"/no/anchor) and the 31-vs-35 reconciliation.
- 2026-09-02 18:5x Stream G rework (Opus, fresh, 57 min + rate-limit cut, 379 k tokens) reported BOTH items: TSK-0113 -- P1 retention per RULE (oldest year + count) + `_named_fairly` round-robin per source (15 duties/tax deadline missing -> 3 duties/tax deadline named; two unit tests + one end-to-end that is red only when BOTH halves are missing -- honestly reported); P2 FP-900/901/002 `retention: null` with reason, test parses the uncommented example block through `_retention_years`; P3 absolute sentence -> pointer to the guard's head, `find -delete` named there; P6 containment by `commonpath`; P10 "DUTY REGISTER UNAVAILABLE"; P5/P7/P8/P9/P11 done (P8: templates as raw lines + pilot name derived from the survey, 0 hits over 36 templates without an exception list); FR-0076 seam: `legal_form` limited to EUeR forms + test that nothing decides on it; `founding_year` deliberately NOT added (no reader; form is I's; no onboarding sentence). TSK-0112 -- `_routine.py` byte-identical x3, office via `_duties.FEEDS`, dev/research call `_routine.notice(cwd)`; measured as a process on scaffolded dev + research pilots; four mutations incl. both mirror ends; dev emission count 22 -> 24. Runs 62/57/734/71/19/78/18 + gates 8; stamps office -15, dev/research -12; patch 24 files (section 13 maps file -> item). Residues: delete FLAG class, clock/timezone, session_brief without PROC section (F), kernel does not validate `retention` (F6), AUDIT_ROLE one direction, F7/F8. Re-verify sent to the same Opus verifier (context kept) for both items.
- 2026-09-02 19:0x Stream H re-verify 3 (Opus, 18 min, 325 k tokens cumulative): **FAIL, 4 findings (B 1, M 1, N 2)**; B1 of round 2 closed (target-bound cell: target sentence removed -> red, unrelated sentence removed -> green), N1-N3 closed, §5a matches the verifier's own runs. New B1: the wishlist entry (:6806-6808) PROMISES `check_limits.py` as a standing guard -- the script is not in the patch (scratch, cleaned at round end) and its new "both ways" cell is word-bound (same claim without "beide" passes). M1 `test_model_pins.py:61` points at a file that does not exist (`verify3/probes/cases3.py`) and `:200` into the verifier's scratch. N1 two wrong section pointers in the N4 fix; N2 a 165-char line. Lead decision: STRIKE the promise (no prose-vs-prose test shipped to tools/; check_limits stays a round instrument named as such in the protocol), fix the two pointers to §5a / the holder test, N1/N2. Rework 4 sent (same Opus implementer). Trajectory H: 12 -> 6 -> 4 findings.
- 2026-09-02 19:1x Stream K re-verify 4 (Opus, 38 min, 310 k tokens cumulative): all 8 findings CLOSED, both blockers with a form that excludes the defect structurally (prefix + running gate test; decomposition + computing instrument, 5 mutations red); package green, stamped -17, in scope, byte-identical with its diff. **FAIL on ONE point:** the measured fence blindness of the hole-list name checker sits in the round protocol as "residue for the merge round" -- CLAUDE.md knows no third state; wishlist in scope, H121 free. Verifier's own chain: planted non-existent name AFTER a fence -> rc 0, BEFORE -> rc 1; 10 of 97 entries carry a fence; LIVE instance H46 (a bare name after a fence that would be a false red under the resolution rule -- two defects cancel); of K's seven prefixes only the four before the fence are guarded. Distinct from I's N11 (resolution target -> false red) -- the fence is the READER -> false green. N-new: nine lines of devlist coverage semantics in the USER's pair document. **DEC-0059 numbers K:** Fable rework 1 -> 5 new findings (5 prose), Fable rework 2 -> 8 (6 prose), **Opus rework 3 -> 2 (0 prose)**, and the Opus rework rejected two claims before writing them. Rework 4 sent (same Opus implementer): write H121 (mechanism, chain, 10/97, H46, the three unguarded prefixes, verdict with the limit "fix in .claude/, until then: prefix after fences"), trim the user paragraph, no stamp if no kit file. Pair document SENT to the user for judgement (table + points frozen).
- 2026-09-02 19:2x USER on the humanizer pair (FR-0072's own test, his judgement): "der Text der bisher rauskam gefaellt mir noch immer nicht" -> the pair does NOT pass the user's taste yet. User asked for a Sonnet research subagent: does a humanizer skill already exist? Spawned (general-purpose, Sonnet, read-only, report to staging/TSK-0111/research-existing-humanizers.md): survey of existing skills (incl. the four MIT sources our SKILL.md names, blader/humanizer, skill collections, German resources), licence, approach (phrase list vs measured properties), German layer; why the after-text may displease (shop copy: warmth, rhythm, reason to buy, the 209 -> 97 cut); three alternative after-versions of the same before-text for the user's taste (external skill rules / German shop-copy style / our skill without the cut); recommendation with effort + licence. K's rework 4 (H121) continues unchanged; the taste verdict is a separate track for the merge decision on FR-0072.
- 2026-09-02 19:3x Stream F verifier (Opus, 58 min, 322 k tokens, two scaffolded pilots): **FAIL, 14 first-pass findings (B 4, M 3, N 7)** (Opus implementer, two runs). B1 `REVISION_WRITES` declared but never registered in `DOCUMENT_WRITES` -> the installed gates still name only apply-proposal ("never a change") = the state before FR-0067 (chain runs in a session; fix in scope, hooks derive); B2 the origin `all(...)` has no test (all->any: 645 passed over seven modules; grandparent case measured) and the named test does not measure it; B3 the document_revision card promises "never as a count" and prints "instruments: 3 Eintraege hinzu" (F2 in the sentence the user signs); B4 `verify-invariants` exits 1 where `validate` exits 0 (undecidable counted as unmet; comment claims they cannot disagree) -> the H110 state moved to the exit code. M1 AST resolution per INV without cache: 7.5-8 s for 30 INV on a 1-MB test file behind a gate registered WITHOUT timeout; M2 `_HINT_WORD` is ASCII-only ('Pruefung der Groesse' -> ['der','fung']; German neighbours 0.625 over the 0.45 band) -- threshold measurement itself CONFIRMED (max 0.314 / n=3601); M3 FR-0040 built without its DEC (no DEC names run_scope; comments name FR/H108, never a DEC). N1 "76 active records" (75 on the base), N2 240 lives twice, N3 contradictory origin refusal in the grandparent case, N4 outline hint printed before a refused capture, N5 the outline has exactly one reader (nothing renders it), N6 §2 route enumeration in all three constitutions needs its own seam (S11), N7 tray test 585 s vs 600 s timeout. Confirmed: BUG-0083 chain as a process incl. dispatch, BUG-0084/0086 both directions, freeze-report refusals + argument list, FR-0037/0039/0054/0067 as processes, 27/29 mutations red, both ratchets, scope/patch/stamp vs both bases, 652/2 skipped, S1 the only red constitution test. Verifier's own miss named: its first mutation selection (-k "origin or root") would have passed all->any. LEAD decisions: B3 render list growth per entry (cap stated as a number of unshown entries, never silent); N5 group by area in kernel/board.py if small, else residue pointing at FR-0075; M3 DEC body as capture-ready JSON in staging/TSK-0106/ -> lead captures via kernel and sends the number; M2 re-measure the band with the Unicode tokenizer. Rework 1 sent to the same Opus implementer (context kept).
- 2026-09-02 19:4x Stream H rework 4 (Opus, 9 min): B1 promise struck (no tool promised in the hole list; check_limits named as a round instrument with its can/cannot in the protocol; 4 target mutations red / 1 control green re-run), M1 both scratch pointers replaced (grep over tools/: only two round-foreign hits), N1 pointers, N2 reflow (max 101/102). Runs 16 / 11 / 8, check_refs 65/0; patch 1946 lines / 6 files. Re-verify 4 sent (same Opus verifier). Stream J rework 1 (Opus, 27 min): B1 summary counted by tool (14 rows = 7/5/2; the prose number was wrong, no row missing); B2 archive walk + docs candidates through `_carried_files` (git) with a new tripwire on a synthetic carrier list + counter-probe (untracked non-ignored file stays red); M1 `assert answered` non-empty + synthetic both-roots in the neighbour test (F5 repair green, reader root loss red in both tests); N1 docstring limit + residue; N2; N3 class renamed CODE (two docs wired only by gates); N4; N5 numbers with tree + time; N6 table "what the lead should enter" with anchors + exact sentence + measured statuses; own finding MR11 (swallowed git failure) -> new test. 16 mutations red; 29 green; full suite NOT re-run (no move changed, said so); patch 54 KB / 4 files applied on both bases; deviation accepted: git-status.txt stays raw (audit line listed, not in the patch). Re-verify sent (same Opus verifier).
- 2026-09-02 19:5x Stream K rework 4 (Opus, 13 min): **H121 written** (fence blindness of the hole-list name checker: mechanism, chain vor/hinter rc 1/0 incl. own prefix made bare -> rc 0, 10/97 via `_hole_entries`, live instance H46 not touched but named as the first thing a reader fix must co-write, verdict OPEN + measured with a human writing rule as the stopgap); the entry itself carries no fence; user doc trimmed to three lines; NO stamp (wishlist + staging only, VERSION -17 x3). Patch 90 KB / 11 files. Re-verify 5 sent (same Opus verifier). Merge note: H121 is a `.claude/hooks/test_gates.py` fix (reader) + H46 prefix -- merge-round scope together with I's N11 (resolution target) -> one fix for the checker, both residues closed at once.
- 2026-09-02 20:0x Humanizer research (Sonnet, 14 min, 166 k tokens) -> staging/TSK-0111/research-existing-humanizers.md, sent to the user. Ten skills surveyed (gh api + fetch, 2026-09-02): German only in two tiny ones (jurigis/avoid-ai-writing-multilingual MIT 13 stars; marmbiz/humanizer-de 133 stars, code MIT / pattern catalogue CC BY-SA 4.0, five Python linters as its core); blader/humanizer 40k stars = phrase list on Wikipedia; nearly every popular one assumes a user dialogue (voice choice / samples) -- confirms the FR-0072 triage. Diagnosis of the user's dislike: the skill did its craft (17 properties moved correctly) but the struck sentences were the ONLY ones carrying a reason to buy -- the 209 -> 97 cut removed AI tics and sales language together because both sat in the same sentence. Three alternative after-texts (a) harshaneel "nine levers", (b) Wortliga Verbalstil with Sie as agent, (c) our skill WITHOUT the cut, 177 words. Recommendation: keep ours, close the gap in property 6/9 ("strike" -> "rewrite when it is the only sentence with a reason to buy"), use foreign catalogues as illustration check only; full adoption would carry copyright/CC-BY-SA duties or contradict the prose-skill decision. Awaiting the user's taste verdict on (a)/(b)/(c) before any FR-0072 change; K's package stays as built for the merge decision.
- 2026-09-02 20:1x **Stream H PASS** (re-verify 4, Opus, 10 min; 0 B / 0 M / 1 N: a line number in the protocol's N2 proof is wrong -- residue, protocol only, fixed in the merge round or left). (g) row H: Opus implementer, two runs (run 1 ~2 h killed at the Stop, run 2 37 min) + four reworks (27 / 27 / 16 / 9 min); verifier first-pass 10 (run 2), then re-verifies 12 -> 6 -> 4 -> 1; total spawn -> PASS ~5 h 30 wall-clock incl. the stop and the rate-limit cut; tokens implementer ~420 k, verifier ~340 k. Patch at `_round-scratch/TSK-0108/stream-measure.patch` (1946 lines, 6 files: tools/test_repo_hygiene.py, tools/test_model_pins.py, docs/reviews/2026-09-02-cd-measurement.md, docs/reviews/2026-09-02-model-pin-and-bom-measurement.md, docs/POST_V2_WISHLIST.md H114-H116, staging/TSK-0108/stream-protocol.md), no kit file, no stamp. Merge notes: wishlist appender (H114-H116) vs E/K/G/I/F appenders; `is_a_definition_a_client_loads` (role = location, skill = SKILL.md name) consistent with K's mirror rule; seams: kit hook for the visible pin fallback (FR-0047 SEAM), README BOM note, CLAUDE.md "binds at session start" sentence, restart ceremony wording.
- 2026-09-02 20:2x **Stream K PASS** (re-verify 5, Opus, 8 min, 0 findings; the "shift persists over fences" sentence attacked with two and three fences -- holds). (g) row K: Fable build 1 h 25 -> 9 findings; Fable rework 1 (36 min) -> 6 (5 new, 5 prose); Fable rework 2 (36 min) -> 8 (6 prose); **Opus rework 3 (31 min) -> 2 (0 prose)**; Opus rework 4 (13 min) -> 0. Total spawn -> PASS ~7 h 50 wall-clock incl. the stop; verifiers: first (tier not logged, died at the stop), then Opus x4 (33 + 51 + 38 + 8 min). Patch `_round-scratch/TSK-0111/stream-humanizer.patch` (90 KB, 11 files) at -17 x3; H120 + H121 written, H122 unused. **FR-0072 status:** package PASSES the verifier; the FR's OWN test (before/after judged by the user) does NOT pass yet -- user: "gefaellt mir noch immer nicht"; research diagnosis: the cut removed the only sentences with a reason to buy; user's taste verdict on three alternative versions pending -> a small FR-0072 follow-up (property 6/9 "strike" -> "rewrite when it is the only sentence with a reason to buy") AFTER the merge, with the user's pick as the acceptance. Merge notes: wishlist appender (H120, H121); I x K collision in `tools/test_hooks.py` docstring (I's four-point resolution); N11 (I) + H121 (K) = one fix in `.claude/hooks/test_gates.py` + H46 prefix.
- 2026-09-02 20:3x Stream J re-verify (Opus, 44 min, 271 k tokens): all ten findings closed and independently re-measured (B2 + three extra cases incl. ignored dirs; F5 repair green; MR3 red in both tests; NULL refs red is CORRECT -- `NONEMPTY_FIELDS` forbids an EVD without artifact_refs; all 13 N6 anchors hit; 15 mutations red incl. MR11 with a real git failure; patch applied on both bases). **FAIL on claim level only: M 1, N 3** -- NF1 `:852` "never blocks a round" false on a tree without .git (4 failed while `:57-63` skips the same question); NF2 "16 Defekte" vs 15; NF3 READMEs promise "really there" while the subject is "what git carries" (unstaged deletion stays green); NF4 "two documents hang only on a gate" overstates (CODE class only; _harness.py is not a gate). Verifier's own misstep named (its backup file inflated the corpus count). No hole to number. Rework 2 sent (same Opus implementer): work-tree skip in `_require_git`, three wordings.
- 2026-09-02 20:4x **Stream I PASS** (re-verify 2, Opus, 1 h 05; 0 B / 0 M / 6 N). All ten findings closed at the root: tax state as a DEFINITION (22 spellings measured, every non-boolean -> unknown, sentence with field + found value + both valid values on every figure tab and in "Jetzt ansteht"); no Zahllast figure or label where applies is false (four states measured); rc != 0; gauge unknown; plural via DOM-derived test; legal basis section 15(2) s.1 no.1 + 15(3) read at gesetze-im-internet (the section 19(1) quote verbatim correct); fixture Einzelunternehmen; N5 both ends. M2 reconciled: 31 vs 35 came from `-k "dashboard"` matching the WORKTREE NAME `g2-dashboard` in six path-parametrised test ids -- not a package property. Six N residues (doc sentence about "Satz 4" -- from MY verifier order, not the code; number words in three comments; dead backtick `vat_statement`; dangling "laufend" on the founding page; no fixture/sighting for the new unknown state; plural test only `-en` and only on founding -> "1 Belege" unseen) -> POLISH round sent to the same Opus implementer (stamp office -16), scoped re-verify after. (g) row I: Fable design 26 min (0 findings, package accepted) + Opus build 1 h 13 -> 13; Opus rework 1 1 h 26 -> 10 (2 B at the fix); Opus rework 2 ~50 min -> 6 N; verifiers Opus 25 min (restart) / 2 h 15 / 1 h 05. Merge notes: I x K docstring collision in tools/test_hooks.py (I's four-point resolution); `main` at 159dcbe -- target `feat/harness-v2` c4f3cc0; office settings.json PreToolUse hooks without timeout (FR-0057); seams G (`kleinunternehmer: null` shipped + interview question; legal form), scripts (validator sentence translation; open-item counting), E (role sentence), K (repo_kit_owned.txt line).
- 2026-09-02 20:5x Stream J rework 2 (Opus, 7 min): NF1 `_require_git` carries the whole question (PATH + work tree), the two older members call it; on a git-archive tree 4 failed -> 15 passed / 6 skipped, MR11 still red; NF2 number struck (MR11 was double-counted); NF3 wording "what the repo carries (git, not the disk -- a deletion shows once staged)", no second disk check by decision; NF4 "both owe their CODE class to no test" (tsk0013 named only by _harness.py, not a gate). 29 green, ruff, validate, no kit file; patch 57 KB / 4 files applied on both bases. Re-verify 2 sent (same Opus verifier).
- 2026-09-02 21:0x Stream G re-verify (Opus, 34 min, 341 k tokens cumulative, three scaffolded pilots): **TSK-0112 PASS** (`_routine.py` 5d857ced x3, notice appears/clears per kit as a process, six mutations red, emission count 24 pinned both ways -- verifier withdrew its own 22); one residue T112-1 (the missing-module test measures dev only while research/dev pointers cite it). **TSK-0113 FAIL on two text findings, no mechanism defect**: all eleven round-1 findings measured closed (P1 3 duties + tax deadline; P2 template live in a pilot 0x INCOMPLETE, reader survives three attacks; P6 all path classes; P8 raw lines over 36 templates + 59 role texts, pilot name derived from office-kit-from-field.md:3, Shopify/BuyPlugGo in a YAML comment red, Gambio/Thermoflasche green = named limit; P11 five boundaries each red; budget worst case 1.15 s / 60 s; H113 corrected; FR-0076 both ends; `founding_year` not added = judged RIGHT). V1 E2E docstring claims "both halves measured here" while either half alone keeps it green (disjunctive coverage accepted; the sentence must say it); V2 office AGENTS.md:141 still claims "every delete" (forbidden scope, correctly untouched, but NOT named as a seam) -> seam E7; V3 `candidate == base` raw-string compare lets `....//<year>/` list the project root (over-report, no escape); V4 audit line in the tree, not in the patch. First-pass findings round 2: 5 (all prose/pointer/coverage). Rework 2 sent (same Opus implementer): V1 sentence + residue, E7 seam, V3 normpath + test, T112-1 parametrise over KITS; office -16.
- 2026-09-02 21:1x **Stream J PASS** (re-verify 2, Opus, 13 min; 0 B / 0 M / 2 N: the `:852` "never blocks a round" sentence unchanged -- absolute still false in two unreadable-repo branches; a line number `:755` vs `:757` in the protocol). Verifier downgraded its own NF1 from B to N after the behaviour fix. Polish sent (same Opus implementer, < 10 min): one subordinate clause + drop the line number. (g) row J: Opus implementer, run 1 ~2 h killed at the Stop, run 2 ~1 h 40 + 7 min after the cut, reworks 27 + 7 min; verifier first-pass 10 (B 2 / M 1 / N 7), then 4, then 2; spawn -> PASS ~9 h 40 wall-clock incl. the stop, the cut and a 2 h full-suite run in three parts; tokens implementer ~340 k, verifier ~290 k. Patch `_round-scratch/TSK-0110/stream-hygiene.patch` (57 KB, 4 files, one rename docs/pilot/2026-08-09-drehbuecher.md -> docs/archive/2026/pilot/, docs/archive/README.md + docs/archive/2026/README.md new, tools/test_repo_hygiene.py +5 tests), no kit file. MERGE WORK from J: `wishlist-pass.md` table "what the lead should enter" (7 thin FRs to capture, 5 pointers with exact sentence + anchor, 2 supersessions) -- lead applies in the merge round; residues F1 (HANDBACK.md patches stale, BUG-0012/0014), F2 (wishlist "zwoelf Dateien" = 15), F3 (445 KB raw logs under docs/reviews), F4 (07-24 review unwired), F5 (artifact_ref two rootings -- store normalisation as a follow-up), N1 corpus = git-carried (merge round numbers), docs/research note stays (user decision per the hole list).
- 2026-09-02 21:2x Stream J polish (Opus, 2 min): `:852` fail-open clause with the two blocking branches named + test; protocol re-measured without the line number. Patch 57 837 B / 4 files / +944/-11. Final check sent (same verifier, 3-sentence verdict).
- 2026-09-02 21:3x **Stream J final PASS, 0 findings** (4 min). Patch final: `_round-scratch/TSK-0110/stream-hygiene.patch` 57 837 B / 4 files / +944/-11, applies on 6d18407 (and c4f3cc0 per re-verify 2). Streams done: E, H, K, J; I awaiting polish check; G (TSK-0112 PASS, TSK-0113 rework 2 running); F rework 1 running.
- 2026-09-02 21:4x Stream G rework 2 (Opus, 15 min, 404 k tokens cumulative): V1 E2E docstring states the disjunctive coverage + residue §10a; V2 seam E7 written (AGENTS.md:141 absolute wall claim; three rc-0 lines; pointer replacement; the same file's :147 already says "about the DOOR, not the wall"; guard absent from dev/research constitutions); V3 `_same_place` (abs, normpath, normcase) for both compares, red-first test for the project-ROOT case, both ends; T112-1 parametrised over the two direct callers (dev, research), research-only mutation red. Runs 75 / 40 / 8; stamp office -16, dev/research unchanged -12; patch 24 files, no project_memory line. Re-verify 2 sent (same Opus verifier).
- 2026-09-02 21:5x Stream I polish (Opus, 18 min, 628 k tokens cumulative): all six residues + the repr note closed; sixth fixture `unclear` (~66 KB, named) rendered and SEEN -> one sighting finding fixed (the long tax sentence stood twice on the Ueberblick); plural test as a derivation over all fixtures (AST plural() pairs + label tuples + data-one/data-many; invariant words allowed only when the corpus shows them), two measurement errors in the test itself found (DOM order; a legal citation is not a count); red-first 5/5, baseline 42; stamp office -16; patch 399 KB / 29 files. Final check sent (same Opus verifier).
- 2026-09-02 22:0x Stream F rework 1 (Opus, 48 min, 618 k tokens cumulative): B1 `REVISION_WRITES` registered in `DOCUMENT_WRITES`, `_owned_elsewhere` reads its own commands from the declaration; measured at the INSTALLED gate (refusal now names both routes); own first test was an identity (read the expectation from the registration it checks) -> replaced by `KIND_BY_COMMAND`; three enumeration pins converted. B2 + N3 grandparent test red with `any`; the first N3 attempt flipped two existing tests -> condition `len(astray) == len(parents) and root_id not in tops`, both halves red separately. B3 one descriptor per list entry on the revision route, refusal above `MAX_PROPOSAL_CHANGES`, never a count. B4 only `resolved is False` counts; undecidable = own line naming H110. M1 cache: 30 INV on 0.5 MB 6.9 s -> 0.22 s, test counts ast.parse calls. M2 Unicode tokenizer + band re-measured on the running reader (98 items, 3772 pairs: honest max 0.314, resubmission min 0.493 -> 0.45 stays) + a second find: short unrelated bugs at 0.500 -> `DUPLICATE_HINT_MIN_WORDS = 20` measured in the empty band (smallest real item 49 words); fixtures replaced by real prose. M3 DEC body written first -> **captured by the lead as DEC-0061** (kernel accepted the five contract fields). N1/N2/N4 done; N5 BUILT (area as a second grouping key in kernel/board.py, 48 board tests, FR-0075 pointer); N6 -> S11 with wording (constitution says in TWO places that replace/delete has no route); N7 named (585 s / 346 s vs 600 s). Runs 660 / 79 + S1 / 8; stamp -16; patch 4575 lines / 27 files. Number insertion + re-stamp -17 requested; re-verify after.
- 2026-09-02 22:1x Stream F: DEC-0061 pointers set at four places, S10 closed, stamp -17 x3, patch 4575 lines / 27 files. KNOWN RED in the worktree: `test_repo_hygiene.test_every_decision_pointer_in_a_shipped_kit_file_resolves` (worktree's decisions end at DEC-0060; DEC-0061 lives in the main tree; green in a copy with both) -- resolves itself in the merge. Re-verify sent (same Opus verifier, 12-point list).
- 2026-09-02 22:2x **Stream I final PASS** (re-verify 3 / round 5, Opus, 35 min; 0 B / 0 M / 1 N). All six residues + note closed; the new fixture yielded a real sighting defect fixed in the same round. One residue for the MERGE item (two lines): the plural derivation trusts the generator's own declaration -- a degenerate pair `("Belege","Belege")` excuses itself, and the `isupper()` clause lets a lowercase bare word through; fix = reject `one == many` at harvest, drop the isupper sieve. Patch final: `_round-scratch/TSK-0109/stream-dashboard.patch` 399 871 B / 29 files / +4826/-6, office -16, applies on 6d18407 and c4f3cc0; 119 PNGs byte-identical. Streams fully done: E, H, K, J, I (+ TSK-0112). Open: F re-verify, G re-verify 2.
- 2026-09-02 22:3x **Stream G PASS -- TSK-0113 AND TSK-0112** (re-verify 2, Opus, 15 min; 1 N residue for the merge item: `_same_place` docstring claims two differences while the separator half is redundant at its call site and the case-fold half is unmeasured -- one sentence or one test case). (g) row G: Opus implementer, run 1 ~2 h killed at the Stop, run 2 84 min, re-cut rework 57 min + cut, rework 2 15 min; verifier first-pass 16 (13 package / 3 item) -> 5 -> 1; spawn -> PASS ~11 h wall-clock incl. the stop, the cut and the item re-cut (TSK-0107 -> TSK-0113 + TSK-0112); tokens implementer ~400 k, verifier ~366 k. Patch final: `_round-scratch/TSK-0113/stream-office.patch` (24 files, both items; office -16, dev/research -12; applies on 6d18407 and c4f3cc0). Seams E1-E7 (constitution/skill sentences incl. AGENTS.md:141 wall claim), F6 (kernel does not validate `retention`), F (session_brief without PROC section), residues (delete flag class, clock/timezone, AUDIT_ROLE one direction, F7/F8, E2E disjunctive coverage). **ALL SEVEN STREAMS PASSED except F (re-verify running).**
- 2026-09-02 22:4x **TSK-0114 captured (DRAFT)** = the generation-2 MERGE round: apply order F, G(0113+0112), I, K, E, H, J on feat/harness-v2; pin tests after every patch; seams by hand (I x K docstring; S1/S11/E1-E7 after E; hole-list appenders H105-H121; residues H122 (K free) + H123+); the hole-list name checker fixed in .claude/hooks/test_gates.py for BOTH limits (N11 + H121) with the H46 prefix; wishlist pointer sentences from TSK-0110; DEC-0061 pointer test green; ONE release stamp; full tools/ suite + test_gates.py in full ONCE before the stamp; protocol with the (g) table. allowed_scope team-kits/**, tools/**, docs/**, NOTICES.md, README.md, CLAUDE.md, .claude/hooks/test_gates.py; forbidden gates/_harness/settings/agents, gen_provider_artifacts.py, scaffold/install/init, user/**, project_memory/**. Dependencies: TSK-0105/0106/0108/0109/0110/0111/0112/0113. -> READY once F passes. Lead's own kernel work after the merge commit: 7 thin FRs + 2 supersessions from wishlist-pass.md, FR status -> MERGED, TSK-0105..0113 CANCELLED (delivered via TSK-0114) + archive, DEC-0057 follow-up with the (g) numbers, BUG-0089 (kernel remedy) queued for F territory.
- 2026-09-02 22:5x Stream F re-verify (Opus, 1 h 22, 412 k tokens cumulative, both pilots re-scaffolded with the new kernel): 12/14 closed by measurement (B1 both routes at the installed gates, expectation from KIND_BY_COMMAND; B2 all->any red; N3 both halves; B3 per-entry card + refusal above the cap, additive card unchanged; B4 rc 0 with undecided line, rc 1 only when refuted; M1 7.5 s -> 0.25 s, parse-count test; M2 German false alarms silent, real resubmission 0.535, band re-measured max 0.314; M3 DEC-0061 four pointers; N4; N5 board grouping rendered on a pilot), N6/N7 converted. **FAIL on ONE B:** the duplicate-threshold comment still claims the named test measures both edges -- after the fixture rebuild + MIN_WORDS the LOWER edge is measured by NOTHING (0.45 -> 0.25 green over 345 tests; was red in round 1) = the fix opened the next hole. M-new: seam S11 quotes a sentence only research carries and claims all three do (office says it differently at :293; dev has no counterpart). N-new: "98 items" (96 on the base), wishlist still points at the "DEC draft" instead of DEC-0061, an over-alarming `== COMMAND` claim measured harmless. Verifier's own misstep named (first B1 probe hit the OLD pilot). Rework 2 sent (same Opus implementer): lower AND upper threshold edge red-first via real pairs (BUG-0070/0071 at 0.314), S11 split into three named places, three N; stamp -18.
- 2026-09-02 23:0x Lead: the seven thin FRs of the TSK-0110 wishlist pass CAPTURED (so the merge round can write "-> FR-00xx" at the anchors): **FR-0077** §1c mechanical hardening halves (axe/keyboard/reduced-motion/focus-visible/colour literals); **FR-0078** §1/1a one-primary-goal gate + SKILL procedure line; **FR-0079** §2 timeline dates as own type MST (DEC first); **FR-0080** §3 plan + mindmap as generated .drawio.svg; **FR-0081** §5 chart of accounts + EUeR mapping, year-versioned (beside FR-0076, not for a GmbH); **FR-0082** §7 evidence result `blocked` + honesty sentence; **FR-0083** §10 approval via Agent SDK canUseTool with token provenance. Pointer table for the merge (from wishlist-pass.md "Was der Lead eintragen soll"): §1b -> FR-0045 (superseded); §1c -> FR-0077; §1/1a -> FR-0078; §2 -> FR-0024 (+FR-0030/0053/0017) + FR-0079; §3 -> FR-0080; §4 -> FR-0047 + DEC-0034 (+DEC-0059); §5 -> FR-0032 + FR-0081; §6 -> FR-0021; §7 -> FR-0082; §8 -> FR-0074 + DEC-0058; §9 -> DEC-0058/FR-0074 (buildable half superseded; writer half stays L1); §10 -> FR-0083. Index regenerated.
- 2026-09-02 23:1x Stream F rework 2 (Opus, 12 min, 649 k tokens cumulative): B-neu = B2 one level up (the old fixture pairs sat at 0.056/0.950, outside the band -> the named test said nothing about the threshold); now three pairs with a task each (0.056 ordinary / 0.329 neighbour = lower edge red at 0.25 / 0.662 resubmission = upper edge red at 0.95); "98 items" removed; S11 split into S11-a (all three), S11-b (research :60), S11-c (office :293) with hit counts; wishlist names DEC-0061; `_owned_elsewhere` sentence states what holds + the 453-green measurement. Runs 659/1 (known DEC-0061 tree split), 8; stamp -18 x3; patch 4589 lines / 27 files. Re-verify 2 sent (same Opus verifier).

## DEC-0060 (g) table -- generation 2, filled 2026-09-02 23:1x (F pending its re-verify 2)

| Stream | Implementer tier(s) | Verifier tier | First-pass findings (B/M/N) | Re-verify trajectory | Rework rounds | Spawn -> PASS (wall-clock, incl. Stop 14:05 + rate-limit cut 17:05) | Prose-class share of findings |
|---|---|---|---|---|---|---|---|
| E texts | Fable | not logged (pre-Stop) | 6 (1 B-as-naming / 2 / 3) | 6 -> 0 | 1 (13.5 min) | ~1 h 55 (finished before the Stop) | high (mirror-floor number, brief count) |
| F kernel | Opus (two runs) | Opus | 14 (4/3/7) | 14 -> 5 (1 B) -> pending | 2 (48 + 12 min) | ~11 h so far | mixed: 4 B were mechanism (unregistered route, untested `all`, counting card, exit code); the B of round 2 was "named test cannot measure" |
| G office | Opus (two runs + re-cut) | Opus | 16 (3 B / 3 hole-list / 7 rework + 3 item) | 16 -> 5 -> 1 | 2 (57 + 15 min) | ~11 h | round 1 mostly mechanism (register accumulation, unreadable template retention, absolute wall claim); rounds 2-3 all prose/coverage |
| H measure | Opus (two runs) | Opus | 10 (4/3/3) | 12 -> 6 -> 4 -> 1 -> 0 | 4 (27/27/16/9 min) | ~5 h 30 | almost all prose (definition vs client, "always", scratch pointers, three versions of one limit) |
| I dashboard | Fable (design, 26 min) + Opus (build + 3 reworks) | Opus | 13 (1/5/7) | 13 -> 10 (2 B at the fix) -> 6 N -> 1 N | 3 (1 h 26 / ~50 / 18 min) | ~9 h | mixed: tax-state one-spelling, second print site, then prose/number words |
| J hygiene | Opus (two runs) | Opus | 10 (2/1/7) | 10 -> 4 -> 2 -> 0 | 3 (27 / 7 / 2 min) | ~10 h (incl. a 2 h full-suite run) | B were mechanism (disk walk vs git, count vs table); the rest prose |
| K humanizer | Fable (build + 2 reworks) -> Opus (2 reworks) | Opus x4 (first not logged) | 9 (2/2/5) | 9 -> 6 (5 new, 5 prose) -> 8 (6 prose) -> 2 (0 prose) -> 1 (H121 residue) -> 0 | 4 (36/36/31/13 min) | ~7 h 50 | Fable reworks: each brought new universal claims; Opus reworks: none |

Reading (for the DEC-0057/0059 follow-up, to be written after the merge): (1) tier did not decide the first-pass count -- Opus streams collected 10-16 findings each, Fable streams 6-9; (2) tier DID decide the rework trajectory in the one stream that switched (K): Fable reworks added prose findings each round, Opus reworks added none; (3) the dominant failure class across ALL streams was "a sentence claims more than the code measures" -- docstrings, hole-list entries, templates, protocol counts -- caught only by an Opus verifier attacking the fix; (4) the two operational incidents (Stop click, rate-limit) cost ~10-20 min per stream on restart but nothing on disk; (5) verifier self-corrections were frequent and named (index incident, wrong pilot, wrong mutation, inflated count) -- a verifier that reports its own missteps is part of why the verdicts held.
- 2026-09-02 23:2x **Stream F PASS** (re-verify 2, Opus, 22 min, 0 findings; both threshold edges red at the named test AND over test_report in full; the three fixture scores recomputed with the running reader 0.056/0.329/0.662; the three neighbour measurements MIN_WORDS 0/60 and ASCII still red; S11-a/b/c hit counts per constitution match; 116 + 44 + 8). (g) row F: Opus implementer, run 1 ~2 h killed at the Stop, run 2 ~3.5 h incl. ~1.4 h test runtime + the cut, reworks 48 + 12 min; verifier first-pass 14 (4/3/7) -> 5 (1 B) -> 0; spawn -> PASS ~12 h wall-clock; tokens implementer ~650 k, verifier ~430 k. Patch final: `_round-scratch/TSK-0106/stream-kernel.patch` 4589 lines / 27 files at -18 x3; known red in the worktree = DEC-0061 tree split (green once merged); seams S1-S11 (S11 a/b/c) + S1 after E; residues H108-H110, N7 (tray test 585 s / 346 s vs 600 s). **ALL SEVEN STREAMS PASSED.** TSK-0114 -> READY; merge implementer (Opus) to be spawned.
- 2026-09-02 23:3x **Merge round spawned** (TSK-0114, Opus implementer, in the repo working tree on feat/harness-v2): apply F, G, I, K, E, H, J with pin tests after each; all provisional VERSION hunks dropped, ONE stamp at the end; seams by hand (I x K docstring, S1/S11a-c/E1-E7 after E, wishlist appenders, residues H122+); hole-list name checker fixed for N11 + H121 in one change with the H46 prefix (red-first both); wishlist pointers with FR-0077..0083; DEC-0061 pointer test green; full tools/ suite once + test_gates.py in full + ruff + validate; protocol with seam table, pointer table, residue numbering, suite numbers and the (g) table. Verifier (Opus) after the report; then lead: EVD via kernel, commit (no push without the user's word), TSK-0105..0113 CANCELLED + archive, FR statuses -> MERGED, DEC-0057/0059 follow-up.
- 2026-09-02 23:5x USER (planning for generation 3, while the merge runs): wants the parallel workflow IN THE KITS too ("genauso wie hier mit Stroemen, dann vereinen, Naht schliessen") = FR-0021 confirmed, derived from DEC-0057/DEC-0060 mechanics; considers FR-0075, 0076, 0072 (follow-up), 0074, 0021 + small items; asks whether 4-5 then 4-5 and whether FRs can be grouped. Lead proposal recorded in the chat: five streams by file ownership -- A Board & Plan (75+79+80, design pass first), B Office Finance (76; 81 deferred), C Approvals & Evidence (74+82+83+BUG-0089), D Parallel specialists (21), E Design gates (77+78); generation 4: 72 follow-up (after the user's taste pick), 57 hook timeouts, BUG-0088, office seams (founding_year/interview question/retention validation), F5 store normalisation, BUG-0077..0087 after triage, the user's later list. Decision pending the user's word after the merge.
- 2026-09-03 00:0x **DEC-0062 VALID** captured from the user's decision: streams cut by FILE OWNERSHIP derived from the paths each FR touches; FRs with overlapping paths GROUPED into one stream (one TSK, primary FR + the others in inputs/outputs, all MERGED at delivery); a stream CAPPED by one round's attention (measured ceiling: F with 11 items / G with 4 FRs, ~4 h build + 1 h verify); every FR keeps its own red test and acceptance line; shared files named as SEAMS at cut time with apply order + arbiter; the same rule goes into the kits with FR-0021; enforcement = the merge round's seam table (an unnamed collision is a finding against the cut). Generation 3 is the first cut under it. The user also asked for a HANDOVER PROMPT for the next session once the merge passes -- to be written to project_memory/staging/TSK-0114/next-session-prompt.md after the commit (state on disk is the authority; the prompt only points).
- 2026-09-03 00:1x **FR-0084 OPEN** captured from the user's reflection ("ginge das auch besser? ... kommt selten von euch"): review bound to EVENTS (phase end, merge/release, repeated finding class, changed decision premise), carried by the project-auditor in the kits (FR-0038/TSK-0112 run record) and the lead's DEC follow-up here, four measured questions, three lines to the user; plus every TSK plan names the rejected alternative in one line (generalising the frontend-design self-check measured in TSK-0109). Bounded: no per-item questioning, taste stays the user's (FR-0074). Candidate for generation 3 stream D (with FR-0021) or generation 4.
- 2026-09-03 03:2x **Merge round (TSK-0114) reported** (Opus, 3 h 43, 494 k tokens, 330 tool uses): seven patches applied F, G, I, K, E, H, J on feat/harness-v2 (c4f3cc0), pin tests green after each (51/53/53/53/53/60/72 + 3), all provisional VERSION hunks dropped, .audit in no patch. Seams: I x K docstring (I's four points; point 3 moot), wishlist re-ordered H105..H121 by script, G x J module docstring, S1 red -> green at all four places, S11-a/b/c + E1-E7 written (E2/E4/E5 in English). Three stream sentences corrected before shipping (S11-b: a list that gains AND loses is ACCEPTED by revision_plan, only an add-only revision is refused; I's office-manager sentence contradicted H117; I's docstring "ships Python" vs .py-or-.html). Name checker fixed in .claude/hooks/test_gates.py for both limits in one change (`_prose_of` strips fences, `_tests_by_module` resolves against tools/test_*.py too, `_cited_test` reads four spellings, H46 prefix; reader now sees 127 instead of 43 citations; six plantings HEAD vs fixed incl. a LIVE false green at H83) -> H121 closed, N11 closed. Residues H122 (git-carried corpus), H123 (delete-flag class at the archive guard, chain re-run rc 0/2), H124 (clock/timezone). Six new red-first tests. Full tools/ suite once in three parts: 2137 + 902/13 sk + 1308/1 sk/1 failed = 4362 = collect; the red = a section pin moved after re-capture -> fixed (86); gate suite full 488 + 1 own comment -> fixed (10). TWO stamp runs (the full run found five findings after the first stamp whose fixes touched kit files) -> final dev/office 2026.09.02-12, research -11; 43 new files add -N. Pattern for DEC-0057: six of eight merge findings were invisible to any stream (contracts cross streams). Named, not closed: I's trigger 6.1/6.2 (H117), F's S2-S6 (H94 open), H's protocol line number, auditor `description` lines. **Merge verifier (Opus) spawned** with a DEC-0050 question: what changed after the full run and did its reading suites run again.
- 2026-09-03 03:3x Handover prompt DRAFTED at `project_memory/staging/TSK-0114/next-session-prompt.md` (commit hash + final stamps to be filled after the commit): reading order (logbook, DEC-0062, FR-0084, brief/index), the five-stream proposal for generation 3 with file ownership, reserved hole numbers (A H125-127, B H128-130, C H131-133, D H134-136, E H137-139), seams named up front, tiers, generation 4 queue, the rules generation 2 measured, and what waits on the user (taste verdict, push, the five-stream answer).
- 2026-09-03 04:2x **Merge verifier (Opus, 1 h 15, 317 k tokens): FAIL, B 1 / M 2 / N 4** -- merge itself clean (no lost hunk: `git apply -R --reject` per patch rejects only VERSION, the wishlist re-order and the named seam files; seam table complete against the measured overlap of 8 files; all eight red-first claims survive own mutations; full gate suite 489 green; 127 vs 43 citations confirmed; 43 add-N files all attributed; scope clean). B1 H123's new limit sentence claims the guard catches the "Versehen" class fully -- measured on a pilot against ALL EIGHT registered office shell hooks: `unlink`, `git clean -fdx` (archive/* is gitignored -> exactly what -fdx removes), `Clear-Content` -> all ALLOW; `DELETE_VERBS` is an enumeration without a wire -> **H125** (open; fix = property instead of verb tuple, a build for generation 3). M1 the repaired checker does not read the summary TABLE rows (ghost in a table row -> false green); M2 inline triple-backtick lines count as fences (even count -> silent). N1 H121 says 10/97, shipped list has 19/116; N2 auditor description vs body sentence (kit file -> residue, gen 3 stream D); N3 "six of eight" not listed; N4 protocol order of full run vs stamps unverifiable (verifier measured the consequences clean: 91/1, 489, 275, 9). Verifier's own missteps named (env var, non-mutation). Rework 1 sent (same Opus implementer): H125 + H123 sentence (prose), checker fixes for table rows + fence definition with plantings A6/A7 then the full gate suite again, N1/N3/N4; N2 deliberately NOT touched (no re-stamp). Hole reservations for generation 3 shift: A H126-128, B H129-131, C H132-134, D H135-137, E H138-140 (prompt draft to be corrected).
- 2026-09-03 06:0x Merge rework 1 (Opus, 1 h 36, 558 k tokens cumulative): B1 chain re-measured on an own pilot (eight hooks), H123 narrowed, **H125 written** (open, "what limits instead: nothing", fix = property instead of verb tuple, gen 3); M1/M2 checker: `_hole_citation_sources` (entry + table row per number, separate corpus so the judges-test stays non-circular), fence = marker-only line incl. `~~~`, pairing by RUN LENGTH, two prose idioms excepted with reason -- plantings A6/A7 rc 0 -> rc 1, A2b/A2c (ghost INSIDE a fence) stay rc 0 = the named price; reader 140 vs 43 citations; N1 both counts (97/10 -> 116/19), N2 residue in §8 (not touched), N3 six findings tabled with test + reason, N4 five post-stamp findings with file list + "kit file?" column (3/5) + the two post-full-run fixes with their reading suites. Gate suite full 489 (first run 488/1 = own docstring with a bare prefix in backticks -> fourth case of that class this round, all four in the protocol); hygiene 25; no kit file, no stamp; three files touched. Re-verify sent (same Opus verifier) incl. full gate suite once more and a DEC-0050 judgement.
- 2026-09-03 07:0x **Merge round PASS** (re-verify, Opus, 2 h 50, 387 k tokens cumulative; 0 B / 0 M / 3 N). Verifier ran test_hooks_v2 FULL (2137), test_hooks FULL (902/13), test_gates FULL (489), hygiene 25, hole/prose tests 12, ruff/validate/--check; H125 chain reproduced on its own pilot (four ALLOW, two rc 2); plantings A6/A7 red, ghost inside a fence = named price, two new plantings red (run-length-2 span; module-looking ghost), prefix exception tight. **DEC-0050 judgement: violated by the letter (full run before the last rework), fulfilled in substance by the verifier's own full runs of every suite reading the files changed after it -> process finding for the DEC-0050 follow-up.** Three N: two stale numbers in the wishlist (116 -> 117 entries; "140 citations" is the undeduplicated measure, the test counts 127), a summary row with a pipe inside a span falls out of the new citation corpus (0 live rows; third price for H121's verdict), protocol §9 vs §8 on the two runs of part A. Text fixes ordered before the commit (implementer, < 10 min, docs + protocol only); then EVD + commit.
