# TSK-0092 close-out protocol (lead, 2026-08-30)

Round: BUG-0070 + BUG-0071 + BUG-0074 + FR-0066 — one construction site: the kernel as the
store's only writer gains the routes the constitutions promise, and stops throwing work away.
Four live finds from the user's real projects in two days (three hand-typed YAML edits, three
wireframes lost to a freeze). Verifier: FAIL(B1-B5+M1-M4) → FAIL(span blocker) → PASS in round 4.

## What ships

- PART A: add-filing-rule CREATES the rules list when a plan carries none (measured on the
  user's real plan minus his hand-typed block; CRLF kept; >1 still refuses).
- PART B: apply-proposal — the generic route over the 18 writerless kit documents. The role
  stages the document as it should stand; the kernel diffs and writes after a user-minted
  approval. The DECISION (generic over per-document commands) is measured: 22 documents, shapes
  that do not rhyme, and the user's own master_data carries fields a kernel schema would have
  refused.
- The three verifier rounds forged its safety at the seam (compare was structural, apply
  bytewise): the SKELETON check — every value blanked via parser spans, every remaining line
  must survive IN ORDER — closes inline-comment loss, comment moves, key reorder, anchors;
  every ADDED comment and every filled/new scalar value stands VERBATIM in the approval card
  (the injected-instruction channel is now disclosed, not silent); a path a NAMED partial
  writer owns refuses with that command's name (the catch-all filing rule cannot sneak past
  add-filing-rule's field-showing card); and the fill span is trimmed to the value's real last
  line, so filling a commented empty list — the round's founding scenario — WORKS (15/16
  accepted, the 16th deliberately refused toward the stricter route).
- PART C: freeze consumes exactly the frozen artifact; the CLI names what remains; the test
  that had PINNED the wipe as intended is flipped. All three freeze commands measured.
- PART D: product-editor gets web with the honest containment note (incl. the surface this
  round itself built); the 27-role survey found no other research-duty role lacking web.

## Verifier rigor worth recording

Four rounds, all probes replayed against every end state; he owned two too-narrow -k runs and
one wrong journal suspicion; the implementer owned two test defects of his own and two relay
truncations were bridged by re-sending short. The final round measured the founding scenario
end to end: the Steuerberatungs-Kategorie fill is accepted at its natural position, and every
attack from three rounds still refuses.

## Hole list

H76 (lead's hand): the list-entry prose channel (the ONE remaining undisclosed-verbatim
channel, triple-bounded); approval reuse within its hour after a manual revert
(filing_correction reading, condition in the docstring); no field schema (deliberate, measured
against the user's own file); prose documents writerless on purpose + three web roles without
the injection note; four safe-direction edges + the fixture-to-repo-audit mechanism.

## Delivery

Kits ×3 2026.08.30-6. ruff, validate clean; mirrors 1 hash each. Suites through the rounds:
474/300/204-baseline mutations all red where claimed. README command-surface paragraph is the
LEAD's edit (authorship asked and answered). Full suite fresh before commit (result in the
commit message). After push: roll out to the global store and tell the user his project update
is worth taking.
