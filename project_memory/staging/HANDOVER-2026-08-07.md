# Übergabe 2026-08-07 — Sitzung steht, bis `state.py` zurückgeholt ist

Diese Datei liegt in `staging/`, weil ich sie schreiben darf und weil der Kernel gerade nicht läuft.
Alles darin muss zu Items werden, sobald er wieder läuft. Was ausschließlich in einem
Sitzungsprotokoll steht, ist verloren — das ist in diesem Projekt nachweislich passiert.

## Der Stillstand

`team-kits/kernel/state.py` ist im Arbeitsbaum zerschrieben: 928 Zeilen, jedes erste `a` je Zeile
ist ein `b`, `SyntaxError` in Zeile 236. Eine Gate-Messung hat ihre Schreib-Nutzlast
(`sed -i 's/a/b/' team-kits/kernel/state.py`, die kanonische Zeile aus `.claude/hooks/test_gates.py`)
gegen das **echte** Repo statt gegen ein Fixture gefahren. Letzte Schreibung 14:39:55.

**Die Index-Fassung ist heil** — 20× `raise StateError`, null Beschädigungen.

Der Sitzungsagent darf `team-kits/` nicht schreiben (Gate 1). Rücknahme aus einer Shell
**außerhalb** von Claude Code, Kopie zuerst:

```bash
cd "c:/Offline Repos/AgentAndSkills"
cp team-kits/kernel/state.py "$TEMP/state.py.zerschrieben-2026-08-07"
git checkout -- team-kits/kernel/state.py
PYTHONPATH=team-kits python -B -c "import kernel.state; print('OK')"
```

### Was dadurch steht

`kernel.cli` importiert `state.py` — also keine Buchführung, kein `validate`, kein `capture`.
Und **Gate 2 verweigert jeden Spawn**: es liest die Itemliste über denselben Import und antwortet
fail-closed. Das ist korrekt und ist die erste Messung dieser Eigenschaft unter echtem Schaden
(`SR-0006`) — sie gehört als Beleg festgehalten, nicht als Ärgernis.

## Was zu Items werden muss, sobald der Kernel läuft

### 1. Prüfung von `TSK-0021` (Apparat, achte Reparatur)

Der Auftrag ist gelandet und ungeprüft. Der Prüfer muss in seiner Kopie `state.py` aus dem Index
zurückholen, sonst misst er den Schaden statt des Pakets — der Umsetzer meldet **134 grün** im Klon
gegen 115 grün / 6 rot / 13 Fehler im beschädigten Baum und belegt, dass dieselben 6+13 auch **ohne**
seine Änderung auftreten.

Die Linien, in der Reihenfolge ihres Werts:

- **Der Riegel gegen die eigene Messvorrichtung.** Abgeleitet: Sandbox bauen, Befüller einmal darin
  laufen lassen, jeden enthaltenen Pfad im Repo hashen. Beide Enden gemessen — sein **erster Entwurf
  blieb grün, während der Ausbruch wirklich schrieb**. Angriff: kann ein Ausbruch stattfinden, ohne
  dass er anschlägt? Er deckt nur Läufe **dieser einen Datei**.
- **Der Defekt in seiner eigenen Installationsvorrichtung**: Edits, deren alte Spanne in der neuen
  steckt, wurden bei jedem Lauf erneut angewandt — drei Läufe, derselbe Block dreimal — während jede
  Prüfung „applied and verified" meldete. Zweites Exemplar dieser Klasse suchen: eine Vorrichtung,
  die Erfolg meldet und etwas anderes tut.
- **Das Tilde-Präfix als Spanne**, Prüfmenge aus dem Alphabet erzeugt (`DEC-0020` angewandt).
- **Die Frist außerhalb der Entscheidung.** Gemessen billiger als angenommen: **66 Zeichen** kosten
  97,7 s; volle Kette 2444 Zeichen → 150,6 s gegen 120 registrierte → getötet → Durchlass. Nachher
  rc 2 nach 96,2 s, unverändert bei 40006 Zeichen. Gegenrichtung angreifen.
- **`H34`, neue benannte Ausnahme**: die Prosa-Entfernung frisst einen gewöhnlichen
  Schreib-Operanden — Shell schreibt, Gate rc 0, ohne Commit und ohne Ersetzung. Reparaturstelle im
  Kit.
- Die Zusammensetzung wird jetzt aus der Tabelle erzeugt; das stille Beschneiden hat einen Draht.

### 2. Nachbesserung der Migration aus der `TSK-0020`-Prüfung (FAIL)

- **F1** — `search_coverage` ist nicht total: ein nicht auflistbares Verzeichnis ist stumm **und**
  nicht blockierend. Und der Stolperdraht kann es nicht sehen, weil sein Orakel derselbe Aufruf ist,
  dessen Blindheit er prüfen soll.
- **F2** — eine andere Schreibweise des Vorschlagsverzeichnisses teilt die eine Antwort wieder in
  zwei: Validator meldet einen Befund, Trockenlauf schweigt. Genau die Eigenschaft, die der Auftrag
  verlangte.
- **F3** — der unbedingte Fang hat **keinen roten Test**; was wirklich wirkt, ist das Byte-Lesen eine
  Zeile darunter. Der Docstring nennt den falschen Mechanismus.
- **Reste** R1–R5: `kernel` als zweite stumme Klasse; jede Prosa-Wand wird als Parserfehler gemeldet
  (trifft die ausgelieferte `masterplan.md` jedes Projekts); die Abhilfe für eine Nicht-YAML-Datei
  unter `staging/` führt ins Leere; die Pfadregel wird für allen Code behauptet und nur in `kernel/`
  gelesen; zwei selbstbezügliche Verweise im Zustandsbaum töten das Merge-Gate.

### 3. Entscheidung, die dem Nutzer gehört

`migration_confirmation_required` steht auf allen importierten Items, wird an drei Stellen
geschrieben und von **keinem** Leser ausgewertet — auch die Spezifikation verspricht keinen. Der
wirksame Riegel ist `approval_ref`, und der **wird** gelesen. Zwei Runden haben das unabhängig
gemessen; der Prüfer nennt es *„eine Markierung, kein Hebel"*. Der Feldname behauptet mehr, als der
Apparat baut: umbenennen, einen Leser bauen, oder als Löcherlisteneintrag führen.

### 4. Löcherlisten-Einträge, die noch abzulegen sind

- `staging/TSK-0018/wishlist-entries.md` — L21, L22, L23 und die Korrektur samt Feldzahl an L17.
- `staging/TSK-0020/wishlist-entry-L19.md` — L19 und zwei Nachzüge.

Beim Einarbeiten muss die Referenz **auch in die Docstrings der genannten Tests**, sonst bleibt das
Paar offen, das der Kopplungstest bewacht.

## Stand des Pakets

Migration: Suite **2305 grün in einem Lauf**, Kits `2026.08.07-4`, `validate.py` sauber, Index
deckungsgleich — gemessen **vor** der Beschädigung, an der Index-Fassung, die der Prüfer unabhängig
bestätigt hat. Beide Feldmigrationen verhalten sich wie entworfen; die ausführbare geht in 61 s mit
285 Items durch, danach `validate`, `doctor`, Index, Sitzungsbrief und Dashboard je rc 0 ohne
Traceback.

Der Prüfer zum dritten Mal: **die Läufe tragen.** Was aufhält, ist die Zusicherung über dem Lauf.
