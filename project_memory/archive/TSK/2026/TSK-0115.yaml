id: TSK-0115
product_requirement: FR-0075
derives_from: FR-0075
type: ui
assigned_role: harness-implementer
acceptance_refs:
- AC-1
allowed_scope:
- team-kits/dev-team/templates/repo/scripts/generate_dashboard.py
- team-kits/dev-team/templates/repo/scripts/progress.dashboard.template.html
- team-kits/kernel/board.py
- team-kits/kernel/backlog_tree.py
- team-kits/kernel/plan_diagram.py
- team-kits/*/VERSION
- tools/**
- docs/**
forbidden_scope:
- team-kits/kernel/backlog_types.py
- team-kits/kernel/cli.py
- team-kits/kernel/state.py
- team-kits/kernel/approvals.py
- team-kits/kernel/documents.py
- team-kits/kernel/report.py
- team-kits/kernel/filing.py
- team-kits/kernel/dispatch.py
- team-kits/dev-team/templates/repo/scripts/kit_*.py
- team-kits/*/constitution/**
- team-kits/*/agents/**
- team-kits/*/skills/**
- team-kits/*/hooks/**
- team-kits/*/settings/**
- team-kits/office-team/templates/**
- team-kits/research-team/templates/**
- scaffold_team.*
- install.*
- init_project_memory.*
- team-kits/gen_provider_artifacts.py
- user/**
- CLAUDE.md
- .claude/**
- project_memory/**
required_inputs:
- project_memory/inbox/active/FR-0075.yaml (primary)
- project_memory/inbox/active/FR-0079.yaml
- project_memory/inbox/active/FR-0080.yaml
- 'project_memory/decisions/active/DEC-0062.yaml (cut rule: grouped FRs, per-FR red
  test, seams named up front) and DEC-0063.yaml (generation-2 verdict: measure every
  new property claim before handover; Fable = design pass only, Opus builds)'
- docs/POST_V2_WISHLIST.md sections 2 and 3 (what FR-0079/FR-0080 point to) and the
  hole-list tail (H125 is the last number taken)
- 'project_memory/staging/TSK-0109/ (README, 01 to 04, user-feedback.md, review/):
  the finance-dashboard design pass and sighting loop this stream repeats for the
  backlog board'
- team-kits/dev-team/skills/frontend-design/SKILL.md and team-kits/dev-team/skills/webapp-testing/SKILL.md
- 'docs/research/2026-07-27-plan-als-diagramm.md (FR-0080: generated, not maintained
  -- already answered there)'
- team-kits/kernel/board.py, team-kits/kernel/backlog_tree.py, team-kits/dev-team/templates/repo/scripts/generate_dashboard.py
  + progress.dashboard.template.html, project_memory/generated/index.yaml (the existing
  data contract; no new writer)
expected_outputs:
- 'TWO-STEP (FR-0075, the shape of TSK-0109): phase 1 = a DESIGN PASS (Fable, frontend-design
  skill) against the brief in FR-0075 -- who reads the board, what they look for first
  (blocked / waiting on the user / in flight), states empty project, healthy, blocked
  -- sighted per the BUG-0076 doctrine (Playwright, 1280 + 390 px, dark, every tab
  and filter), recorded under project_memory/staging/(this task id)/; the pass MEASURES
  whether kernel/board.py and the dev-team HTML board ever disagree on the same numbers
  and recommends one renderer with two outputs or two renderers, with reason; it names
  in one line the alternative brief it rejected and why. Phase 1 ends with a report
  to the lead. Phase 2 (Opus, second spawn) builds on the existing generator with
  the existing data contract (generated/index.yaml + rollups): no new writer, no new
  field unless the design pass measured a missing one and reports it as a seam; kept
  from FR-0030: refresh trigger, kit ownership, one-file property.'
- 'FR-0079 (MST): phase 1 recommends TYPE or FIELD with the kernel consequences spelled
  out (backlog_types AUTOMATA / ACTIVE_DIRS / REQUIRED_FIELDS, index, board timeline);
  the DEC is the USER decision, captured by the lead before phase 2 starts. If TYPE:
  phase 2 renders MST on the board timeline from a fixture, and the type-definition
  lines for backlog_types.py are a SEAM to stream C (verbatim in the protocol with
  the test each needs; the lead hands them to C during the round; applied in C or
  in the merge, never here). If FIELD: the field is rendered and the DEC named beside
  it. Either way FR-0079 keeps its own red-first test and acceptance line.'
- 'FR-0080: implementation plan and mindmap generated from the items (index / rollups)
  as .drawio.svg, regenerated by the SAME trigger as the board (the one call site
  that regenerates the board; if that site is in a file this stream does not own --
  kernel/cli.py is C-owned -- the one call line is a seam, verbatim); a hand edit
  of a generated diagram is detected by a test; nothing is hand-maintained. If a module
  of its own is needed it is team-kits/kernel/plan_diagram.py and nothing else in
  the kernel.'
- Per FR its own red-first test measured in a copy outside the repo (restore the defect,
  see red, put it back) and its own acceptance line in the protocol; the verifier
  reports PER FR (DEC-0062 (4)). Every new property claim -- code comment, docstring,
  docs, hole list -- is MEASURED before handover (the most frequent finding of generation
  2); every hole-list citation carries its module prefix; hole numbers H126 to H128
  ONLY; forbidden_scope project_memory/** excepts project_memory/staging/(this task
  id)/.
- 'Seams reported, never written here: constitution / role / skill sentences the new
  board or diagrams need -> stream D (verbatim in the protocol); kernel type or trigger
  lines -> stream C; tools/test_hooks.py mirror-dir tests (shared with stream B: files
  derived from templates/repo dirs shipping *.py); docs/POST_V2_WISHLIST.md appended
  only under the reserved numbers.'
- 'Handover: worktree C:/Offline Repos/v2-testbed/_worktrees/g3-board (branch g3/board
  off feat/harness-v2 at e45c0ca); ALL scratch only under C:/Offline Repos/v2-testbed/_round-scratch/(this
  task id)/; verifier copies made WITHOUT the .git file of the worktree; patch = the
  worktree diff at C:/Offline Repos/v2-testbed/_round-scratch/(this task id)/stream-board.patch;
  protocol at project_memory/staging/(this task id)/stream-protocol.md with: seam
  table, per-FR acceptance line + red-first tests, measured lines, what was deliberately
  not closed but named, the one-line rejected alternative of the plan (FR-0084 shape),
  suite runs (affected suites only -- the full suite belongs to the merge round),
  provisional VERSION stamp, wall-clock and tokens for the (g) table. No commit, no
  push, no install to the global store.'
dependencies: []
root_revision: 1
status: CANCELLED
revision: 1
approval_ref: null
created: '2026-09-03T07:21:06'
closed_at: '2026-09-04T02:50:05'
