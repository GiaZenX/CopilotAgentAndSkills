id: TSK-0043
product_requirement: PR-0002
derives_from: BUG-0020
type: bugfix
assigned_role: harness-implementer
acceptance_refs:
- AC-2
- AC-3
allowed_scope:
- team-kits/**
- tools/**
- docs/reviews/**
- docs/POST_V2_WISHLIST.md
forbidden_scope:
- .claude/**
- CLAUDE.md
- project_memory/**
- user/**
required_inputs:
- 'BUG-0020 echte Wurzel = H34, gemessen im Handback 2026-08-11: gate_write_scope._MESSAGE_ARG_RX
  (team-kits/dev-team/hooks/gate_write_scope.py:132-135) ist mit re.IGNORECASE uebersetzt
  und die Alternative enthaelt -F, matcht also -f; die quotierte Spanne dahinter wird
  als Prosa entfernt, BEVOR ein Gate liest. Gemessen mit bash als Schiedsrichter:
  rm -f "project_memory/decisions/active/DEC-0001.yaml" ist rc 0 und loescht das Item
  wirklich (so ist DEC-0001 diese Nacht verschwunden); dieselbe Zeile ohne Anfuehrungszeichen
  rc 2, ohne -f rc 2. Loeschen SELBST ist erfasst (rm/mv/Remove-Item/del/git rm/find
  -delete alle rc 2) -- die Prosa-Entfernung nimmt den Operanden weg. Auch cp x -b
  "geschuetzter-pfad" ist rc 0 (gleiche Klasse). Verwandt H21/H22/H24.'
expected_outputs:
- 'Die quotierte Spanne wird an das VERB gebunden statt an die Flagschreibweise: nach
  einem schreibfaehigen Verb (rm/mv/cp/Remove-Item/...) ist ein quotierter Pfad-Operand
  ein Schreibziel, kein entfernbares Prosa-Argument -- rm -f "geschuetzter-pfad" wird
  rc 2, in JEDER Flagschreibweise (-f/-F/kombiniert). Gegenrichtung ohne neues Ueber-Verweigern:
  eine echte Nachrichten-Flag-Nutzung (git commit -m "prosa") bleibt erlaubt. Gemessen
  mit bash als Schiedsrichter, echter Hook-Prozess, beide Enden. Ein Test wird ohne
  den Fix rot (extern belegt), deckt rm/mv/cp und mehrere Flagschreibweisen. Byte-identisch
  ueber die drei Kits gespiegelt, VERSION-Bump. Falls die Klasse nicht vollstaendig
  schliessbar ist, steht der Rest mit gemessener Kette und Begrenzung in docs/POST_V2_WISHLIST.md
  (H34 aktualisiert)'
dependencies:
- TSK-0039
root_revision: 1
status: CANCELLED
revision: 1
approval_ref: null
created: '2026-08-11T14:55:27'
closed_at: '2026-08-13T09:08:29'
