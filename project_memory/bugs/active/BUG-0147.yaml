id: BUG-0147
title: Die Alt-Bestand-Brücke läuft ohne gemintete Freigabe, und ein Subagent erreicht
  sie — offen (neu, TSK-0081)
related_pr: PR-0003
observed: '**Mechanismus:** Im Alt-Bestand (bis Kit 2026.08.14-9) existiert die Freigabeart
  `kit_update`

  nicht — es gibt nichts zu minten, und kein Hook dort kann erzwingen, dass der PM
  den Nutzer

  fragt. Die Brücke (`~/agents-and-skills/update_kit.py`) liest ihren Aufrufer nicht,
  weil ihre

  Eingabe keine Payload trägt; das Alt-`gate_write_scope` verweigert Subagenten nur
  die

  **ordnenden** Kommandos, und eine Bootstrap-Zeile ist keines.'
severity: medium
hole_number: H55
source: docs/holes/H55.md
limits: 'die Update-Brücke im Alt-Bestand läuft auf ein gesprochenes Ja statt einer
  geminteten Freigabe, und auch ein SUBAGENT erreicht sie (Alt-Gate: bridge rc 0,
  scaffold rc 2) — die Brücke liest ihren Aufrufer nicht, weil ihr niemand eine Payload
  gibt. Begrenzung: nur Alt-Bestand (im gehobenen Projekt verweigert sie jedem, gemessen
  rc 2); keine Argumente, Richtungs- und Staging-Hash-Prüfung, Backups, `project_memory`
  unberührt (gemessen byte-identisch)'
status: TRIAGED
revision: 1
approval_ref: null
created: '2026-09-05T20:24:01'
