# TSK-0038 — Verifier verdict (BUG-0007 ruff reproducibility), 2026-08-11

## Verdict: PASS (after a one-line rework the verifier specified)

### Substance (round 1, measured green)
- Explicit rule set chosen not inherited: `ruff.toml` `[lint] select = ["E4","E7","E9","F"]`; the
  inherited default gave 0 effective findings today, so does the explicit set — status quo frozen.
  Measured drift: select-less 0.15.20 → 37, 0.16.2 → 3439; WITH select both → 0. The fix is carried
  by the select, not the per-file-ignore (0.16.2 select-less = 3439 with and without the ignore).
- **No real defect masked** (the key risk): verifier ran `--select F` → 0; `B006/B008` → 0; broader
  sets yield only style/policy (E501 24815, ANN/S/UP/PTH/D). One benign B023 (false-positive in a
  green test file) and two cosmetic PLE2510 (backspace chars in gate_ledger_valid.py — captured
  separately as BUG-0030). The narrow select genuinely freezes the status quo, hides nothing.
- ruff pinned at one place: `.github/workflows/ci.yml:28` `"ruff==0.15.20"` (no root pre-commit /
  requirements / pyproject). Tripwire tests read the running parts (tomllib/YAML parse, real ruff
  subprocess), both ends: no-select → red, bare-ruff / `ruff>=` → red, pin≠installed → honest skip,
  ignore-removed → red.

### The one blocking finding, fixed
`tools/test_ci_lint_pinned.py::_ruff_pins` used `_RUFF_PIN.search(line)` (first match per line only),
while `test_ci_pins_exactly_one_ruff_version`'s docstring promised to catch "a second, disagreeing
pin" including on one line. Measured: two pins on one `pip install` line → test stayed green.
Fixed: `search` → `finditer`; new `test_two_disagreeing_pins_on_one_line_are_both_seen` monkeypatches
`_ci_run_lines` to one synthetic line and calls the real `_ruff_pins()`. Red-without-fix proven in an
external clone (search → `['0.15.20']`, assert breaks; finditer → both seen).

### Acceptance
`pytest tools/ -q` → 2451 passed / 13 skipped one run; ruff clean; validate rc 0; `bump --check`
unchanged (only `.github`/`ruff.toml`/`tools/` touched — not kit-hash inputs).

### Named residues (non-blocking)
1. pyyaml/pytest in the same CI line stay unpinned — outside BUG-0007 (ruff-specific); pytest drift
   affects suite reproducibility, deserves its own item eventually.
2. Kit downstream: `templates/repo/.pre-commit-config.yaml` (rev v0.6.9) AND `requirements-dev.txt`
   (bare ruff) can diverge in a generated project — a kit design point, not this repo's CI.
3. Pin is 0.15.20, not 0.16.x — deliberate (installed here, so the executing test runs real not
   skips); the select is green at 0.16.2 too, so a later bump is a safe, deliberate decision.
