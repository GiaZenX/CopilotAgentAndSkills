# TSK-0051 (BUG-0005) — Verifier verdict: the session brief carries the last decisions, 2026-08-12

## Verdict: PASS

Measured against a frozen tree in a copy outside the repo; the real project_memory read-only.

## The three ACs, measured
- **The brief carries the newest standing decisions with title + decision.** Real
  `generate-session-brief` over the actual project_memory: 36 active DEC → `standing_decisions`
  shows exactly the 5 newest, newest-first over `(created, id-number)` with a stable id-number
  tiebreak on equal seconds. `test_session_brief_carries_the_newest_standing_decision` green.
- **A test measures a fresh decision in the brief** — green. **The test goes red when the section is
  removed** — proven (three mutations: drop the field → SchemaError; drop the status filter →
  status-superseded test fails; `item_required` [id,title] → the required-keys test does not raise).
- Superseding measured BOTH ways (one source `_holding_decisions`, report.py:1237): a link-superseded
  DEC and a status-SUPERSEDED DEC are both absent; a holding decision that supersedes another is
  present. `_DEC_IN_FORCE_STATUS` derived from `NON_AUTOMATON_STATUSES["DEC"][0]`, not hardcoded.

## Budget, backward-compat, suite
- Bounded: 5 decisions × 400 chars, `…`-clipped; the real brief measured 6726 bytes < 25600, validate
  OK. The section alone cannot blow the budget; the pre-existing unbounded-other-lists gap is not
  worsened materially (named residue).
- Backward-compat: `validate` against the real project_memory → 0 errors / 0 warnings; the schema
  field is additive (version 1→2); briefs are regenerated per session so no persisted old brief is
  re-validated.
- Clean full suite against the frozen tree: **2494 passed, 13 skipped, 0 failed**. The section pin is
  genuinely green (the three SKILL work-loops now name "the newest standing decisions", re-pinned via
  pin_constitution_sections.py — JSON baseline + journal only, no code path). The `id_scan_is_linear`
  timing test was green. The verifier's earlier 62-failure runs were its own sandbox artefacts
  (PYTHONPATH shadow, missing .git, missing user/radar) — none touched the brief; the targeted run
  was clean from the start (90 passed).

## Non-blocking note (not a fix)
`standing_decisions()` now returns a status-SUPERSEDED-but-not-link-superseded DEC in neither
`standing` nor `superseded` — it vanishes from both returns. The function has no production caller, so
this is harmless; if it ever gets one, such a DEC would be invisible there. Pure note.

## Why this matters
BUG-0005 is the loss class the whole harness is built against, one level up: what stood only in the
conversation was gone after a restart. It also feeds BUG-0019 — the phase-2 PM fell back to the raw
transcript partly because the brief was incomplete; a brief that now carries the last decisions
reduces that pull.
